# CLAUDE.md

Guidance for AI coding agents working in this repository (socrasteeze's fork of SwarmUI).

**[AGENTS.md](/AGENTS.md) is the canonical conventions document — read it before coding**, along with [CONTRIBUTING.md](/CONTRIBUTING.md) and any relevant skill files in `.agents/skills/`. This file only summarizes the highest-traffic rules and adds fork-specific policy.

## What this project is

SwarmUI is a modular AI media generation web UI, in three parts: a JS/HTML browser frontend, a C# ASP.NET Core server (the main workhorse), and a Python backend (mostly upstream ComfyUI, auto-downloaded into `dlbackend/`) that runs the actual AI execution.

## Repo map

- `src/Core/` — server core: `Program.cs` (entry point), `WebServer.cs` (routes, theme registry, extension page injection), `Extension.cs` + `ExtensionsManager.cs`, `Settings.cs`
- `src/WebAPI/` — HTTP/WebSocket API routes (reflection-registered via `API.RegisterAPICall`)
- `src/Backends/` — generation backend abstraction; the main ComfyUI backend lives in `src/BuiltinExtensions/ComfyUIBackend/`
- `src/Text2Image/` — generation domain model (params, models, presets, prompt handling)
- `src/Accounts/` — users, sessions, roles, permissions
- `src/Pages/` — Razor pages: `Text2Image.cshtml` (main app page + script load order), `_Generate/GenerateTab.cshtml` (main tab layout), `Shared/_Layout.cshtml` (head/meta)
- `src/wwwroot/js/genpage/` — main app JS: `main.js`, `gentab/layout.js` (responsive/mobile layout), `gentab/params.js`, `gentab/currentimagehandler.js` (image viewers), `helpers/`
- `src/wwwroot/css/` — `genpage.css`, `site.css`, `themes/` (theme CSS; `modern.css` is the shared base)
- `src/BuiltinExtensions/` — extensions tracked in git and compiled into the main assembly. **Fork-owned code goes here.**
- `src/Extensions/` — **gitignored** user-downloaded extensions. Never put fork-tracked code here.
- `launchtools/` — install/build/docker scripts (handle with maximum caution)
- `docs/` — documentation; `docs/APIRoutes/` is autogenerated, do not edit
- `Data/`, `Output/`, `Models/`, `dlbackend/`, `src/bin/`, `src/obj/` — runtime/user data, never touch

## Build / run / verify

- Build: `dotnet build src/SwarmUI.csproj --configuration Release`
- Lint (CI-enforced): `dotnet format SwarmUI.sln --verify-no-changes`
- Headless boot check: `./launch-linux.sh --ci-test true --launch_mode none --loglevel debug`
- Dev run: `./launch-linux-dev.sh` (rebuilds every launch); serves on `http://localhost:7801`
- CI: `.github/workflows/build-and-check.yml` (build + format check + headless boot)
- **No automated tests.** Validate by static reasoning plus manually running the live app in a browser.

## Convention quick-reference (full rules in AGENTS.md)

- **C#**: never `var` (explicit types); full braced blocks always; `///` XML docs on all fields/methods/properties; use FreneticUtilities helpers (e.g. `ToLowerFast`) before reimplementing.
- **JS**: always `let`, never `var`/`const`; prefer `==` over `===`; `else {` on its own line; full braced blocks; standard `for` loops, not `.forEach`; `/** */` docs on functions; new code uses class-based singletons (`class FooHelper {...} fooHelper = new FooHelper();`); check `util.js`/`site.js` for existing utilities first. No bundler — scripts are ordered `<script>` tags (genpage scripts registered in `Text2Image.cshtml` `@section Scripts`), cache-busted with `?vary=@Utilities.VaryID`.
- **CSS**: class selectors only (`.foo`), never id selectors (`#foo`). Must work on modern Chrome/Firefox/Safari, desktop and mobile.

## Agent access on this fork

Upstream's AGENTS.md restricts agentic development to an approved-maintainers list. This repository is socrasteeze's personal fork, and the fork owner authorizes agent-written code on fork branches. Respect upstream's policy boundaries:

- Do NOT edit AGENTS.md's maintainer list.
- Do NOT open PRs to upstream (`mcmonkeyprojects/SwarmUI`) containing agent-written code without following CONTRIBUTING.md's LLM-Written Code policy.
- Upstream's "agents never run builds" rule is relaxed here: agents may run `dotnet build`, `dotnet format`, and the ci-test boot to validate changes.

## Merge-friendly fork policy (fork law)

This fork periodically merges upstream master (`git remote add upstream https://github.com/mcmonkeyprojects/SwarmUI`, then `git fetch upstream && git merge upstream/master`). To keep merges clean:

1. Prefer **new files** under `src/BuiltinExtensions/<FeatureName>/` (builtin-style extension: plain folder, `.cs` class extending `Extension`, no `.csproj`, auto-discovered).
2. Use extension hooks instead of editing core: `ScriptFiles`/`StyleSheetFiles`/`OtherAssets` for asset injection; `OnPreLaunch()` with `WebServer.WebApp.MapGet(...)` for routes and `WebServer.PageHeaderExtra` for `<head>` additions; JS callback arrays `sessionReadyCallbacks`, `featureSetChangedCallbacks`, `postParamBuildSteps`, `hideParamCallbacks`.
3. Core-file edits only as a last resort, kept minimal/append-only, and always recorded in the Fork Delta below.
4. After every upstream merge: re-run the verify gate (build + format + ci-test boot) and re-check the coupling watchlist in the mobile/PWA plan doc.

## Fork Delta (living list — keep updated)

Files this fork adds or changes relative to upstream:

- `CLAUDE.md` (this file)
- `launch-fork.bat` — fork launcher wrapper. Fetches `upstream` and flags any unmerged commits with a banner (review/merge commands shown, no auto-merge), then delegates to `launch-windows.bat` unmodified. Run this instead of `launch-windows.bat` directly. In-app restarts (exit code 42) re-enter `launch-windows.bat`'s own restart loop and intentionally skip the upstream check.
- `docs/MobilePWA-Optimization-Plan.md` — implementation plan for the mobile/PWA overhaul (phases, verified design facts, verification gates, and the coupling watchlist to re-check after upstream merges)
- `src/BuiltinExtensions/MobileEnhancements/**` — mobile/PWA extension (Phases 1–4 implemented: PWA manifest + service worker, touch image viewer with swipe navigation, mobile layout/ergonomics CSS, network resilience; plus a Civitai share-to-download target: PWA `share_target` → `/ShareTarget` route → `mobile_share.js` prefills the Model Downloader; plus a native app shell (`mobile_shell.js` + shell CSS in `mobile.css`): a fixed bottom tab bar (Create/History/Models/More) and a slide-up parameters drawer that reuse the core Generate DOM via `.click()`/`genTabLayout.setBottomShut`, scoped to `body.small-window.mobile-shell-active`, opt-out via the More sheet's "Classic Layout" toggle. The shell reserves its own space by growing `body`'s `padding-bottom`, which `reapplyPositions()` already subtracts from every height — so it needs **no** core layout edit). Behavioral couplings tracked in the plan doc's coupling watchlist. **Not yet browser-verified** — build/format/boot could not run in the authoring environment (egress policy blocked the .NET SDK download); validated by static analysis + JS/JSON syntax checks. Run the verification gate before relying on it.
  - `mobile_core.js` also tags the "Simple" and "Comfy Workflow" top tab links with a `.mobile-optional-tab` class (`tagOptionalTopTabs()`, run once from the constructor) since neither has a distinguishing class of its own to hide by; `mobile.css` hides `.mobile-optional-tab` under `body.small-window` — with `!important`, because upstream's `comfy_workflow_editor_helper.js` `featureSetChangedCallbacks` handler force-shows the Comfy Workflow tab via inline `style.display = 'block'` on every comfyui backend (re)connect (frequent on PWA resume), which otherwise wins over the class rule. Both remain reachable on mobile - Simple via the More sheet's "Simple Mode" button (a plain `.click()`, unaffected by `display:none`), Comfy Workflow via desktop/the layout tab-config panel.
  - Safe-area cap (2026-07, after on-device testing): iOS standalone PWA reported `env(safe-area-inset-bottom)` ~100px on the target iPhone (the physical-pixel value of the 34pt home indicator, ~3x the correct CSS-px value), ballooning a huge dead band under the bottom nav. All bottom-inset math in `mobile.css` now flows through `--safe-bottom: min(env(safe-area-inset-bottom), 34px)` (defined on `body.pwa-standalone, body.small-window`; 34px = largest legitimate iPhone value) — never consume the raw bottom `env()` directly in new rules. Also: the parameters drawer gained a `.mobile-drawer-grip` invisible strip over its pill handle (drag-down or tap always dismisses; the older in-content swipe only arms when the sidebar is scrolled to the very top, which its persisted scroll position rarely satisfies).
  - PWA polish pass (2026-07): the shell's bottom nav derives all its safe-area math from one var (`--shell-nav-inset` = `max(env(safe-area-inset-bottom) - 10px, 0px)` on the shell body rule) so the icons overlap 10px of the home-indicator inset instead of floating the full 34px above the bezel — tune only that var; body padding, nav height, Options FAB, and drawer bottom all derive from it, and `layout.js` live-reads body's computed padding-bottom so height math follows free. The classic-layout Generate-row inset rule (`.alt-prompt-buttons-wrapper`) is scoped `:not(.mobile-shell-active)` — with the shell active it double-stacked a second inset of dead space above the nav bar. `sw.js` enables navigation preload (activate handler + `event.preloadResponse` in the navigate branch, guarded for Safari). `manifest.json` adds `"id": "/"` and `launch_handler: navigate-existing` (app-icon/share launches focus the running instance instead of spawning a new one).
  - `mobile.css` also caps `.alt-prompt-added-image-area` (the pasted-image-prompt thumbnail strip) to `max-height: 5.5rem` with `overflow-y: auto`, and shrinks `.alt-prompt-image` to `max-height: 4.5rem`, both scoped to `body.small-window`. Previously each pasted image could add up to 128px with no ceiling, and since it lives inside `alt_prompt_region` inside `.main-image-area` (which clips overflow instead of growing), enough pasted images pushed the actual prompt textboxes/Generate button out of the visible, clipped-off area rather than making anything scroll. See also the `layout.js` floor below for the matching center-image-area fix.
- `src/Pages/Shared/_Layout.cshtml`, `src/Pages/Text2Image.cshtml`, `src/Pages/Login.cshtml`, `src/Pages/Register.cshtml`, `src/Pages/Install.cshtml`, `src/Pages/GoogleOAuthVerify.cshtml`, `src/Pages/ThemePreview.cshtml`, `src/Core/WebServer.cs` — **core-file edits** (attribute-only, low risk). Added `defer` to every `<script src=...>` tag app-wide (head scripts, body-end scripts, per-page Scripts sections, and the extension-injected script tag generated in `WebServer.cs`), so the browser can parse and paint the DOM — including the prompt textarea — before executing ~30 JS files, instead of blocking on them sequentially. `defer` preserves each script's original document-order execution relative to every other deferred script, so load order (and thus every downstream dependency like jQuery-before-genpage-scripts) is unchanged — only the timing relative to DOM paint moves earlier. Addresses mobile's slow-to-interact prompt box on fresh load; does not address the separate, heavier main-thread burst in `main.js`'s session-ready callback (double parameter-DOM build, full-document translation walk, several `select2()` inits) — that was scoped out as higher-risk and left for a future pass.
- `src/wwwroot/js/genpage/gentab/layout.js` — **core-file edit** (last resort, no extension hook exists to modify a core `document`-level touch listener). Upstream's swipe gesture (added in commit `5ab12da6`) toggled the left/right tool sidebars AND the bottom bar (LoRAs etc.) from one combined touch handler, and its touch-start filter only excluded `BUTTON`/`INPUT` targets — not `TEXTAREA` or contenteditable — so selecting/highlighting prompt text was misread as a swipe. Fix: kept the vertical swipe (up opens the bottom bar, down closes it back to the image view) since that's a used feature, removed only the horizontal left/right sidebar swipe, and added `TEXTAREA`/`isContentEditable` to the touch-start exclusion so text selection no longer triggers either. Also removed one redundant synchronous `reapplyPositions()` call from `init()` (a second call moments later already covers it), and the swipe-up-to-open trigger zone (bottom 1/6 of the screen) now subtracts the bottom safe-area inset from its reference height, since that padding (added for the PWA bezel fix, see MobileEnhancements below) is dead space that had shrunk the effective grabbable zone without the JS threshold accounting for it. Same root cause also clipped the bottom bar's content (including the always-visible current-LoRAs strip): `reapplyPositions()` sizes the top section and bottom bar off raw `100vh`/`49vh` with no bottom-inset subtraction, so their normal-flow heights summed to slightly more than the actually-visible content box, and `body`'s `overflow: hidden` silently clipped the excess off the bottom bar. Fixed by subtracting the same live-read safe-area-bottom padding from every `100vh`/`49vh`-based height calc in that function. Re-check this area after upstream merges in case upstream reworks the same gesture. **Perf pass (mobile typing jank):** added `scheduleReapply()` — a `requestAnimationFrame`-coalesced wrapper around `reapplyPositions()` — and routed the two prompt-textarea `input` handlers and the window `resize` handler through it, so a burst of keystrokes or iOS's continuous visualViewport keyboard-resize events collapse to one layout pass per frame instead of one synchronous full relayout per event. Also wrapped the `eval()`-based advance-width computation (and its `getComputedStyle`) in a `!isSmallWindow` guard, since its only consumer (the `<400px` min-width nudge) is already mobile-excluded — so on a phone that dead computation no longer runs on every pass. Desktop behavior is unchanged except that the same-frame calls are now batched. **Center-image-area floor:** `currentImageWrapbox`/`editorSizebar` height is computed as available space minus `altHeight` (the floating prompt bar's own live height, i.e. text boxes plus any pasted images); wrapped both calcs in `max(6rem, ...)` so that when `altHeight` grows larger than the space actually left over (easy on a short mobile viewport, especially with several pasted images — see the matching `mobile.css` cap above), the image/batch preview area — and its "Image"/"Batch" tabs — floors out at 6rem instead of collapsing to 0 while the ancestor's `overflow:hidden` simultaneously clips the prompt textboxes/Generate button out of view. Coupling watchlist: `reapplyPositions` internals + the input/resize call sites.
- `src/wwwroot/js/genpage/gentab/params.js`, `src/wwwroot/js/genpage/gentab/presets.js`, `src/wwwroot/js/genpage/main.js` — **core-file edits** (low risk, additive). Initial page load no longer builds the "New Preset" modal's duplicate parameter-panel DOM (`genInputs(false, false)` in `main.js`) — it's built lazily on first use of that modal (`ensurePresetInputsBuilt()` in `presets.js`). Halves the parameter-DOM-build/select2-init work on every load for sessions that never open that modal; every other `genInputs()` caller is unaffected (parameter default preserved).
- `src/wwwroot/js/genpage/helpers/browsers.js`, `src/wwwroot/js/genpage/helpers/ui_improvements.js` — **core-file edits** (low risk, matches an existing pattern used elsewhere in the same files). On iOS Safari, `position: fixed` popovers (the browser thumbnail-card action menu, and the thumbnail-backed Model/VAE/LoRA dropdown replacement) were appended as children of panels that have `-webkit-overflow-scrolling: touch` (`.input-sidebar`, `.browser_container`, added for momentum scrolling in the MobileEnhancements extension). That CSS property makes WebKit create a new containing block, which traps `position: fixed` descendants inside it instead of letting them float over the whole page — the popup rendered clipped/covered instead of on top. Fixed by appending both popovers to `document.body` instead, matching how other `AdvancedPopover` call sites in the same files already do it (and matching that class's own documented recommended usage). That fix introduced a follow-on bug in `browsers.js`: the thumbnail-card popover's ID is index-based (`${browserId}-${index}`), and it used to get destroyed for free whenever the browser list rebuilt (`contentDiv.innerHTML = ''` on every navigate/refresh) since it was a child of that container; once moved to `document.body` it survives rebuilds, so a later render reusing the same index left a stale popover (bound via closures to the old file at that position) sitting in the DOM ahead of the fresh one — `getElementById` resolves to whichever exists first, so opening a card's menu after any list refresh could show/act on the wrong LoRA, preset, or model. Fixed by removing any existing element with that ID immediately before creating the new one.
