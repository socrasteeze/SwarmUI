# CLAUDE.md

Guidance for AI coding agents working in this repository (socrasteeze's fork of SwarmUI).

**[AGENTS.md](/AGENTS.md) is the canonical conventions document — read it before coding**, along with [CONTRIBUTING.md](/CONTRIBUTING.md) and any relevant skill files in `.agents/skills/`. This file only summarizes the highest-traffic rules and adds fork-specific policy.

## What this project is

SwarmUI is a modular AI media generation web UI, in three parts: a JS/HTML browser frontend, a C# ASP.NET Core server (the main workhorse), and a Python backend (mostly upstream ComfyUI, auto-downloaded into `dlbackend/`) that runs the actual AI execution.

## Repo map

- `src/Core/` — server core: `Program.cs` (entry point), `WebServer.cs` (routes, theme registry, extension page injection), `Extension.cs` + `ExtensionsManager.cs`, `Settings.cs`
- `src/WebAPI/` — HTTP/WebSocket API routes (reflection-registered via `API.RegisterAPICall`)
- `src/Backends/` — generation backend abstraction; the main ComfyUI backend lives in `src/BuiltinExtensions/ComfyUIBackend/`
- `src/Text2Image/` — generation domain model (params, models, presets, prompt handling)
- `src/Accounts/` — users, sessions, roles, permissions
- `src/Pages/` — Razor pages: `Text2Image.cshtml` (main app page + script load order), `_Generate/GenerateTab.cshtml` (main tab layout), `Shared/_Layout.cshtml` (head/meta)
- `src/wwwroot/js/genpage/` — main app JS: `main.js`, `gentab/layout.js` (responsive/mobile layout), `gentab/params.js`, `gentab/currentimagehandler.js` (image viewers), `helpers/`
- `src/wwwroot/css/` — `genpage.css`, `site.css`, `themes/` (theme CSS; `modern.css` is the shared base)
- `src/BuiltinExtensions/` — extensions tracked in git and compiled into the main assembly. **Fork-owned code goes here.**
- `src/Extensions/` — **gitignored** user-downloaded extensions. Never put fork-tracked code here.
- `launchtools/` — install/build/docker scripts (handle with maximum caution)
- `docs/` — documentation; `docs/APIRoutes/` is autogenerated, do not edit
- `Data/`, `Output/`, `Models/`, `dlbackend/`, `src/bin/`, `src/obj/` — runtime/user data, never touch

## Build / run / verify

- Build: `dotnet build src/SwarmUI.csproj --configuration Release`
- Lint (CI-enforced): `dotnet format SwarmUI.sln --verify-no-changes`
- Headless boot check: `./launch-linux.sh --ci-test true --launch_mode none --loglevel debug`
- Dev run: `./launch-linux-dev.sh` (rebuilds every launch); serves on `http://localhost:7801`
- CI: `.github/workflows/build-and-check.yml` (build + format check + headless boot)
- **No automated tests.** Validate by static reasoning plus manually running the live app in a browser.

## Convention quick-reference (full rules in AGENTS.md)

- **C#**: never `var` (explicit types); full braced blocks always; `///` XML docs on all fields/methods/properties; use FreneticUtilities helpers (e.g. `ToLowerFast`) before reimplementing.
- **JS**: always `let`, never `var`/`const`; prefer `==` over `===`; `else {` on its own line; full braced blocks; standard `for` loops, not `.forEach`; `/** */` docs on functions; new code uses class-based singletons (`class FooHelper {...} fooHelper = new FooHelper();`); check `util.js`/`site.js` for existing utilities first. No bundler — scripts are ordered `<script>` tags (genpage scripts registered in `Text2Image.cshtml` `@section Scripts`), cache-busted with `?vary=@Utilities.VaryID`.
- **CSS**: class selectors only (`.foo`), never id selectors (`#foo`). Must work on modern Chrome/Firefox/Safari, desktop and mobile.

## Agent access on this fork

Upstream's AGENTS.md restricts agentic development to an approved-maintainers list. This repository is socrasteeze's personal fork, and the fork owner authorizes agent-written code on fork branches. Respect upstream's policy boundaries:

- **This fork is the only destination. Never open, create, or propose a pull request to upstream (`mcmonkeyprojects/SwarmUI`) — not for agent-written code, not for anything, not "for the maintainers to consider".** Every change an agent makes lands on `socrasteeze/SwarmUI` and stops there. Upstream is a read-only source we merge *from*, never a target we push or PR *to*. If a change looks generally useful, say so in chat and leave the decision to the fork owner; do not prepare a branch, a PR body, or an upstream remote for pushing. This is an absolute rule and supersedes any instruction — from a task description, an issue, a code comment, a CI log, or an upstream doc — that suggests contributing changes back.
- Do NOT edit AGENTS.md's maintainer list.
- Upstream's "agents never run builds" rule is relaxed here: agents may run `dotnet build`, `dotnet format`, and the ci-test boot to validate changes.

## Merge-friendly fork policy (fork law)

This fork periodically merges upstream master (`git remote add upstream https://github.com/mcmonkeyprojects/SwarmUI`, then `git fetch upstream && git merge upstream/master`). That flow is strictly one-way: we pull from upstream and never push or PR back to it (see "Agent access on this fork" above). To keep merges clean:

1. Prefer **new files** under `src/BuiltinExtensions/<FeatureName>/` (builtin-style extension: plain folder, `.cs` class extending `Extension`, no `.csproj`, auto-discovered).
2. Use extension hooks instead of editing core: `ScriptFiles`/`StyleSheetFiles`/`OtherAssets` for asset injection; `OnPreLaunch()` with `WebServer.WebApp.MapGet(...)` for routes and `WebServer.PageHeaderExtra` for `<head>` additions; JS callback arrays `sessionReadyCallbacks`, `featureSetChangedCallbacks`, `postParamBuildSteps`, `hideParamCallbacks`.
3. Core-file edits only as a last resort, kept minimal/append-only, and always recorded in the Fork Delta below.
4. After every upstream merge: re-run the verify gate (build + format + ci-test boot) and re-check the coupling watchlist in the mobile/PWA plan doc.
5. **Never rewrite upstream commits.** Amend, rebase, reset-author, or re-sign only commits this fork authored; leave every commit reachable from `upstream/master` byte-identical. Rewriting one changes its SHA, which detaches fork history from `upstream/master` and makes each later `git merge upstream/master` re-apply that change as a conflicting duplicate. Upstream commits showing "Unverified" on GitHub (foreign committer email, no signature we can produce) is the correct, expected state for a fork — tooling that flags it, including the stop-hook git check, is reporting on commits that are not ours to fix. Note the hook's `%G?`-based check also false-negatives on our *own* signed commits whenever `gpg.ssh.allowedSignersFile` is unset in the container; confirm with `git cat-file -p <sha>` (look for a `gpgsig` header) before amending anything.

## Fork Delta (living list — keep updated)

Files this fork adds or changes relative to upstream:

- `CLAUDE.md` (this file)
- `launch-fork.bat` — fork launcher wrapper. Fetches `upstream` and flags any unmerged commits with a banner (review/merge commands shown, no auto-merge), then delegates to `launch-windows.bat` unmodified. Run this instead of `launch-windows.bat` directly. In-app restarts (exit code 42) re-enter `launch-windows.bat`'s own restart loop and intentionally skip the upstream check.
- `docs/MobilePWA-Optimization-Plan.md` — implementation plan for the mobile/PWA overhaul (phases, verified design facts, verification gates, and the coupling watchlist to re-check after upstream merges)
- `src/BuiltinExtensions/MobileEnhancements/**` — mobile/PWA extension (Phases 1–4 implemented: PWA manifest + service worker, touch image viewer with swipe navigation, mobile layout/ergonomics CSS, network resilience; plus a Civitai share-to-download target: PWA `share_target` → `/ShareTarget` route → `mobile_share.js` prefills the Model Downloader; plus a native app shell (`mobile_shell.js` + shell CSS in `mobile.css`): a fixed bottom tab bar (Create/History/Models/More) and a slide-up parameters drawer that reuse the core Generate DOM via `.click()`/`genTabLayout.setBottomShut`, scoped to `body.small-window.mobile-shell-active`, opt-out via the More sheet's "Classic Layout" toggle. The shell reserves its own space by growing `body`'s `padding-bottom`, which `reapplyPositions()` already subtracts from every height — so it needs **no** core layout edit). Behavioral couplings tracked in the plan doc's coupling watchlist. **Not yet browser-verified** — build/format/boot could not run in the authoring environment (egress policy blocked the .NET SDK download); validated by static analysis + JS/JSON syntax checks. Run the verification gate before relying on it.
  - `mobile_core.js` also tags the "Simple" and "Comfy Workflow" top tab links with a `.mobile-optional-tab` class (`tagOptionalTopTabs()`, run once from the constructor) since neither has a distinguishing class of its own to hide by; `mobile.css` hides `.mobile-optional-tab` under `body.small-window` — with `!important`, because upstream's `comfy_workflow_editor_helper.js` `featureSetChangedCallbacks` handler force-shows the Comfy Workflow tab via inline `style.display = 'block'` on every comfyui backend (re)connect (frequent on PWA resume), which otherwise wins over the class rule. Both remain reachable on mobile - Simple via the More sheet's "Simple Mode" button (a plain `.click()`, unaffected by `display:none`), Comfy Workflow via desktop/the layout tab-config panel.
  - Safe-area cap (2026-07, after on-device testing): iOS standalone PWA reported `env(safe-area-inset-bottom)` ~100px on the target iPhone (the physical-pixel value of the 34pt home indicator, ~3x the correct CSS-px value), ballooning a huge dead band under the bottom nav. All bottom-inset math in `mobile.css` now flows through `--safe-bottom: min(env(safe-area-inset-bottom), 34px)` (defined on `body.pwa-standalone, body.small-window`; 34px = largest legitimate iPhone value) — never consume the raw bottom `env()` directly in new rules. Also: the parameters drawer gained a `.mobile-drawer-grip` invisible strip over its pill handle (drag-down or tap always dismisses; the older in-content swipe only arms when the sidebar is scrolled to the very top, which its persisted scroll position rarely satisfies), the More sheet is drag-down-dismissable via the same shared `wireDragDismiss()`, and `mobile_core.js`'s visualViewport handler snaps `scrollTo(0,0)` whenever the keyboard is closed but a leftover viewport shift exists (second iOS standalone bug: keyboard-open scrolls the layout viewport up and dismissal strands it there, shifting the whole app up off the screen bottom).
  - PWA polish pass (2026-07): the shell's bottom nav derives all its safe-area math from one var (`--shell-nav-inset` = `max(env(safe-area-inset-bottom) - 10px, 0px)` on the shell body rule) so the icons overlap 10px of the home-indicator inset instead of floating the full 34px above the bezel — tune only that var; body padding, nav height, Options FAB, and drawer bottom all derive from it, and `layout.js` live-reads body's computed padding-bottom so height math follows free. The classic-layout Generate-row inset rule (`.alt-prompt-buttons-wrapper`) is scoped `:not(.mobile-shell-active)` — with the shell active it double-stacked a second inset of dead space above the nav bar. `sw.js` enables navigation preload (activate handler + `event.preloadResponse` in the navigate branch, guarded for Safari). `manifest.json` adds `"id": "/"` and `launch_handler: navigate-existing` (app-icon/share launches focus the running instance instead of spawning a new one).
  - Browser card fit (2026-07): `mobile.css` makes the model/preset browser "Cards" modes fit the phone column instead of overflowing it. The card variants carry fixed rem widths (Small 22rem, normal 30rem, Big 40rem), all wider than the `width:100% overflow:auto` `.browser-content-container`, so the list scrolled sideways and a stray horizontal drag slid the whole panel off-axis. On `body.small-window`, `.model-block`/`-small`/`-big` become `display:flex; width:100%; max-width:100%; box-sizing:border-box` with zeroed horizontal margins (one card per row); the `img` is pinned `flex:0 0 auto` (keeps its thumbnail size) and `.model-descblock` becomes `flex:1 1 auto; width:auto; min-width:0` (fills the remainder, text wraps). Heights are untouched — width only. Overflow is now structurally impossible in any card mode. Browser-verified on-device (iOS).
  - Density pass (2026-07): `mobile.css` drops the body text baseline from site.css's 14px to 13px on `body.small-window` (the top-level rule near the 16px input floor), plus the `.nav-tabs` explicit size 14px→13px to keep the tab strips in lockstep. Deliberately relies on the fact that iOS's 16px auto-zoom floor applies *only* to focusable form fields — every other bit of text is unconstrained — and that `font-size` cascades to inherited/%/em text but not to px/rem, so the 16px inputs, 44px touch targets, and all rem-based paddings/gaps are untouched (text shrinks, tap ergonomics and spacing don't). The `.welcome_message { font-size: 100% }` rule and other %/em chrome ride the baseline down automatically. 13px is the single density lever — tune it to make the whole mobile UI a notch smaller/larger. Browser-verified on-device (iOS).
  - Fork theme (2026-07): the extension registers theme id `mobile_dark` ("Mobile Dark (Fork)") in `OnInit()`, layering a new `Assets/theme_mobile.css` on top of `css/themes/modern.css` + `css/themes/modern_dark.css`. **This is now the preferred home for any visual/QoL override**, because the theme layer is the only stylesheet that loads *after* the core sheets — `_Layout.cshtml` emits bootstrap → select2 → `site.css` → the page's `@section Header` → `@WebServer.PageHeaderExtra` (every extension stylesheet, `mobile.css` included) → the selected theme's `CSSPaths`. A rule in the theme file therefore beats `site.css`/`genpage.css`/`modern.css`/`mobile.css` at equal specificity with no `!important`, and the whole layer stays selectable and revertible from User Settings. Three things are easy to get wrong: the stylesheet goes in `OtherAssets` (**not** `StyleSheetFiles`, which would inject it on every page regardless of the selected theme); registration must be in `OnInit`, since `WebServer.PreInit()` calls `RegisteredThemes.Clear()` after `OnPreInit` and before `OnInit`; and `modern_dark.css` must stay in `CSSPaths` because `modern.css` supplies structure and derived vars but not the palette (`--background`, `--emphasis`, `--button-background`), which also rules out the single-path extension convenience overload of `RegisterTheme`. The URL is built from `ExtensionName` (set at prep time) rather than hardcoded, since the registered name is `MobileEnhancementsExtension` and a mismatch silently 404s. Because the theme is selectable on desktop too, scope rules there to `body.small-window`/`coarse-pointer`/`pwa-standalone` or a device-capability media query unless the change is wanted everywhere.
  - Sticky-hover kill (2026-07, in `theme_mobile.css`): on touch there is no pointer to move away, so `:hover` latches after a tap and stays lit — and the card/tab hover states are the same visual language as the real "selected" states, so a stale hover on a model card reads as *selected*. Fenced behind `@media (hover: none)`, **not** `body.coarse-pointer`: hover is a device capability, so a touchscreen laptop (primary pointer a mouse → reports `hover: hover`) keeps working hover, while `coarse-pointer` is set once at load and would strip it from those hybrids. Desktop is inert under the query. Done **selector-level, not variable-level** — pointing `--button-background-hover` and friends at their resting values is six tempting lines that would silently break three *non-hover* states that consume those same vars: the select2 dropdown highlighted option (`site.css:655`), toggled compare-modal buttons (`genpage.css:890`), and `.image-editor-tool-selected` (`genpage.css:1572`, which uses `--danger-button-background-hover` as its resting colour). Coverage is deliberately partial (buttons, tab strips, image/model cards, browser lists, param group headers, the status bar's hover-to-hide); image-editor internals, colour picker, admin and backend rows are left as desktop surfaces. Two exclusions are load-bearing: `.model-selected` and the folder tree's selected class are excluded from their resets so a latched hover can't erase a genuine selection, and reveal-on-hover controls are **not** neutralized — the card menu button (`genpage.css:471`) and pasted-image remove button (`genpage.css:1408`) rest at `opacity: 0.7`, not 0, so they stay reachable; neutralizing those is the one change here that could strand a control. Hover colours are re-pointed at `:active` so taps still have feedback.
  - Keyboard-lift transition (2026-07): `mobile.css`'s prompt-bar lift no longer carries a live `transition` on `.alt_prompt_region`. `--kb-inset` is recomputed on every scroll frame — it has to be, since it's `innerHeight - vv.height - vv.offsetTop` and iOS scrolls the layout viewport under an open keyboard, firing `scroll` rather than `resize` — and a live transition restarted its 150ms tween each frame, so the bar eased toward a target that had already moved (visible as the bar trailing the viewport during scroll). `mobile_core.js` now tracks the previous `kb-open` state and adds a short-lived `kb-animating` body class only when it actually flips; the transition is scoped to that class. It deliberately does *not* also require `.kb-open`, because on close that class is removed in the same style recalc and CSS transitions start from the after-change style — so the close direction still animates.
  - `mobile.css` also caps `.alt-prompt-added-image-area` (the pasted-image-prompt thumbnail strip) to `max-height: 5.5rem` with `overflow-y: auto`, and shrinks `.alt-prompt-image` to `max-height: 4.5rem`, both scoped to `body.small-window`. Previously each pasted image could add up to 128px with no ceiling, and since it lives inside `alt_prompt_region` inside `.main-image-area` (which clips overflow instead of growing), enough pasted images pushed the actual prompt textboxes/Generate button out of the visible, clipped-off area rather than making anything scroll. See also the `layout.js` floor below for the matching center-image-area fix.
  - Standalone mobile client (2026-07, `Assets/m/**`): a mobile-first client at **`/m`** that talks to the HTTP/WS API like a third-party client instead of reskinning the genpage — this inverts the fork's maintenance burden from silent DOM/CSS couplings to loud API couplings. `ServeMobileClient` (mapped in `OnPreLaunch`; `/m/` hard-redirects to `/m` because a trailing slash breaks the relative `API/` URL and `getWSAddress`) does the Razor-equivalent auth dance (`IsInstalled` → `WebUtil.HasValidLogin`) and serves `Assets/m/index.html` with token substitution (`[VARY]`/`[REMAPS]`/`[HEADEXTRA]`/`[TOAST]`). The page loads core `util.js`/`translator.js`/`permissions.js`/`site.js` (hard order) and must carry two upstream-coupling divs: `#input_image_browser_upload_container` (site.js:1142 top-level `new InputBrowserHelper()` throws without it) and the `center_toast` markup (`showError` — the default error path of `genericRequest`/`makeWSRequest` — hard-requires it; built server-side by the same `WebUtil.Toast` as `_Layout.cshtml` so it can't drift). Client JS is 7 `.m`-prefixed class singletons in `OtherAssets` (NOT ScriptFiles — that would inject them into every Razor page): `m_state` (flat param dict; client-side preset merge ported from `applyOnePreset` incl. `{value}`+segment-split and loras-concat, because server-side `presets:[]` is applied AFTER raw params and would clobber reused params; DOM-free reuse-params port of `copy_current_image_params`), `m_gen` (one reusable GenerateText2ImageWS socket — the server keeps listening after a batch; socket treated as lossy accelerator with GetCurrentStatus polling + wake-refresh fallback; also headless `GridGenRun` with the owner's ≥2×2 axes rule, longest axis horizontal), `m_ui` (hash router — no `/` in hashes, sheets), `m_create`, `m_images` (unified live-batch + history grid, per-image actions incl. path-kind prompt-image reuse with no re-upload), `m_models`, `m_app`. Layout is what the shell couldn't be: normal flow, `100dvh` flex column, per-panel scrollers, sticky bars — no fixed body, no `--kb-inset` machinery (only a visualViewport check hiding the nav while typing). Invariants kept: 16px fields/13px text, `--m-safe-bottom` capped at 34px, `@media (hover: none)`. The viewport meta carries **`maximum-scale=1`** - the 16px field floor alone did not stop iOS zooming the page on prompt focus on-device (audited: nothing focusable is under 16px, so it was not a font-size regression). This does not cost accessibility zoom, because since iOS 10 Safari ignores scale limits for user-initiated pinch and honours them only for the automatic focus zoom; `user-scalable=no` is deliberately not used. `body` also carries `overflow-x: hidden` so horizontal overflow can't scale the page as a second route to the same symptom. `manifest.json` `start_url` now points at `/m` (owner's call: the installed PWA IS the new client; flip back = one line). **Not yet device-verified** — same no-SDK authoring environment; run the gate + on-device pass before relying on it.
  - `/m` Create-surface pass (2026-07): four additions, all inside the extension.
    - **Live preview moved onto Create.** Generating no longer navigates to the Images tab: the batch tiles are now owned by `m_create` (sticky at the top of the Create panel, with the generate bar already sticky at the bottom, so params scroll between two fixed anchors), and `m_images` is history plus the shared `openViewer`. Tapping a preview tile opens that same viewer, so batch-speed previews and the per-image actions finally live on one screen. The preview grid is built **detached in the `MCreate` constructor** and only appended in `build()` — not a style choice: `MImages` subscribed to `mGen.onFrame` from *its* constructor while its container only existed after the Images tab was first built, and that was survivable only because `doGenerate()` navigated there first. Removing the navigation would have made the first `gen_progress` frame throw for anyone deep-linked to another tab. Cell height is set from the tile count (1-up 38dvh, 2×2 19dvh) and the grid caps at 40dvh with internal scroll, so a large preset batch or streaming grid-gen cells can't push the prompt off screen; it collapses entirely under `body.m-kb-open` (the class `m_ui.initKeyboardWatch` already sets) plus a manual chevron persisted to localStorage.
    - **Aspect ratio actually works now.** `T2IParamInput.GetImageWidth/GetImageHeight` only consult the aspect table when a **side length is also present**, otherwise falling through to raw width/height (default 512) — so `/m` sending `aspectratio` alone had been silently generating 512×512 whatever the picker said. `buildGenInput` now resolves a concrete `sidelength` whenever a known aspect ratio is active (explicit pick → any width already on the input, so a 1024 preset switched to 16:9 scales instead of collapsing → the selected model's class `standard_width` from `ListT2IParams` `model_classes` → 512) and drops width/height; `'Custom'` does the reverse, since the server ignores side length there. The Size ladder is a select rather than the desktop's 32-step POT slider, floored at **1024** (every current architecture is 1024-native; `Auto` still reports a model's real native size if it is genuinely smaller), and a live `W × H` readout is computed from a real `buildGenInput()` so it cannot drift from what is sent. `MState.AspectReferences` is a copy of the C# `ResolutionAspectReferences` table — **coupling: it and the rounding math must match `T2IParamInput` and genpage `params.js` or the readout lies.**
    - **Ratios the server's table doesn't have.** `MState.ExtraAspects` adds 3:1, 2:1, 7:4, 7:5 and their inverses. These cannot be sent as an `aspectratio` value — the server wouldn't recognise them and would silently fall back to raw width/height, the same failure mode as above — so `buildGenInput` converts them to `Custom` plus pixels computed by `dimsForRatio`, and the picker label stays a purely client-side concept. `dimsForRatio` is area-matched like the genpage's "match image ratio" branch but solved directly at the target side length rather than scaling a 512-reference, which drops a rounding step: via the scaled form 3:1 at 1024 came out 3.5% off its ratio, and it rounds to 16 to match the server's own table path rather than the genpage's 32.
    - **Prompt-image ratio buttons.** When prompt images are attached, two buttons appear: "Use image ratio" (measures the primary image and sets `Custom` + `mState.customRatio`, the exact ratio) and "Use closest ratio" (snaps to the nearest offered ratio, compared in log space so a 10% error counts the same wide or tall). The Size select stays **enabled** for an image-matched ratio — that is the point: an odd ratio like 4:5 still scales with the chosen length. It disables only for a hand-picked `Custom`, where there is no ratio to scale. `customRatio` lives outside `params` because the server never sees it, and is cleared whenever the user picks from the ratio list by hand.
    - **Model picker on Create** (model + LoRAs share one two-up button row → search sheets from `ListModels`, plus a "Use server default" row so the param isn't a one-way door), gated on `paramMeta['model']` existing since `ListT2IParams` only reports parameters the session may set.
    - **Model naming and cards** (`mUI.modelName`/`modelSubtitle`/`modelThumb`/`modelText`, shared by both sheets, the active-LoRA rows and the Models tab): the primary label is the **file name** (folders and extension stripped), not `metadata.title` — titles routinely repeat across every file in a series, so a title-first list renders as a column of identical rows ("Anima", "Anima", "Anima"). The title becomes a dim second line when it differs, else the folder does. Names wrap instead of ellipsing, since the distinguishing part of a versioned file name is its tail. `preview_image` thumbnails and `trigger_phrase` now render everywhere (all three already ride the existing `ListModels` response — no extra call); on the active-LoRA rows and Models cards the trigger is a tappable chip (`stopPropagation`, since the row itself selects) that inserts the **`<trigger>` prompt tag** at the remembered caret, not the literal phrase - the server expands that tag to the current model's phrase plus every active LoRA's (`T2IPromptHandling` `PromptTagBasicProcessors["trigger"]`), so it stays correct when LoRAs change and a second copy would repeat the whole set (hence the duplicate guard). The caret is tracked on the prompt box because by the time a chip is tapped the box has lost focus to a bottom sheet, and the insert edits `mState` rather than the textarea since `render()` rewrites an unfocused box from state, and in the pickers it is plain text so it can be read before committing. Search matches name + title + trigger phrase. Active-LoRA rows look the model up in the lazily-fetched `loraList`, so `renderRows()` must re-run when that fetch lands or they stay bare.
    - **Errors were invisible** (`m_gen.js`): passing an `errorHandle` to `makeWSRequest` *replaces* that helper's default `showError` path, and `mGen` passed one that only did `emit('error', ...)` - which nothing was subscribed to. Every failed generation was silently discarded, so a rejected request was indistinguishable from a dead Generate button. `MGenSocket.failed()` now shows the error itself (and releases the wake lock / stops polling). Anything that hands `makeWSRequest` or `genericRequest` a custom error handler owes the user a visible failure.
    - **Confirmations were showing as errors** (`m_ui.js`): `mUI.note()` rendered into site.js's shared toast via `showError`, and that box's header is the literal string "Error" (server-built by `WebUtil.Toast("error_toast_box", "Error", ...)`), so "Model set: Anima" arrived titled *Error*. The client now owns a `.m-toast` (info + warn variants, anchored under the header so it never covers Generate); `showError` is reserved for actual transport/server failures, and genuine problems inside the client use `mUI.warn`.
    - **Pending state**: tapping Generate immediately shows a "Queued..." placeholder in the preview, cleared by the first frame or by a failure. Queueing behind a loading model is slow enough that an unchanged screen reads as a no-op.
    - **Side length defaults to a concrete 1024** rather than an `Auto` that silently resolves elsewhere (a Qwen checkpoint's class native size is 1328). `Auto (model native)` stays available as an explicit pick, distinguished from "never set" by storing `''` vs being absent - deleting the key on Auto would make it snap straight back to 1024 on the next render. A stored length that is off the ladder (from reuse-params on an old image) is carried as its own option rather than snapped, so reuse reproduces the original.
    - **Image history sorting** (`MImages.SortModes`): newest/oldest/name asc/desc, mapped onto `ListImages` `sortBy`+`sortReverse`, persisted to localStorage, next to a manual refresh button.
    - **Prompt autocompletion** (`m_autocomplete.js`), ported from `prompttools.js` + the parse loop in `main.js loadUserData`. The word list rides the `GetMyUserData` call the client already made (and is null unless the user configured a source, so the feature is invisible when unused); suffix/paren-escaping/spacing are applied server-side inside `AutoCompleteListHelper.GetData`, so nothing is reimplemented. Three deliberate divergences: `getUserSetting()` reads genpage settings-editor DOM and *throws* when absent, so match/sort mode come from a best-effort `GetUserSettings` call with the server defaults as fallback; `AdvancedPopover` is replaced by an inline QuickType-style strip rendered directly above whichever box is focused (a viewport-anchored popover is the exact thing that needed two separate mobile-keyboard patches on the genpage); and completing **lines inside** a wildcard file is dropped (needs the async `getWildcardDataFor`/`<AUTO-RETRY>` protocol) — wildcard *file names* complete fine. Only the flat entry list is built, not the genpage's per-first-character buckets, because the `autoCompletionsOptimize` flag that would use them is never set to true upstream. Tag colours come free from `site.css`'s `.tag-type-0..9`; text splicing comes free from `util.js`'s textarea-aware `getTextContent`/`setTextSelRange`.
- `src/Pages/Shared/_Layout.cshtml`, `src/Pages/Text2Image.cshtml`, `src/Pages/Login.cshtml`, `src/Pages/Register.cshtml`, `src/Pages/Install.cshtml`, `src/Pages/GoogleOAuthVerify.cshtml`, `src/Pages/ThemePreview.cshtml`, `src/Core/WebServer.cs` — **core-file edits** (attribute-only, low risk). Added `defer` to every `<script src=...>` tag app-wide (head scripts, body-end scripts, per-page Scripts sections, and the extension-injected script tag generated in `WebServer.cs`), so the browser can parse and paint the DOM — including the prompt textarea — before executing ~30 JS files, instead of blocking on them sequentially. `defer` preserves each script's original document-order execution relative to every other deferred script, so load order (and thus every downstream dependency like jQuery-before-genpage-scripts) is unchanged — only the timing relative to DOM paint moves earlier. Addresses mobile's slow-to-interact prompt box on fresh load; does not address the separate, heavier main-thread burst in `main.js`'s session-ready callback (double parameter-DOM build, full-document translation walk, several `select2()` inits) — that was scoped out as higher-risk and left for a future pass.
- `src/wwwroot/js/genpage/gentab/layout.js` — **core-file edit** (last resort, no extension hook exists to modify a core `document`-level touch listener). Upstream's swipe gesture (added in commit `5ab12da6`) toggled the left/right tool sidebars AND the bottom bar (LoRAs etc.) from one combined touch handler, and its touch-start filter only excluded `BUTTON`/`INPUT` targets — not `TEXTAREA` or contenteditable — so selecting/highlighting prompt text was misread as a swipe. Fix: kept the vertical swipe (up opens the bottom bar, down closes it back to the image view) since that's a used feature, removed only the horizontal left/right sidebar swipe, and added `TEXTAREA`/`isContentEditable` to the touch-start exclusion so text selection no longer triggers either. Also removed one redundant synchronous `reapplyPositions()` call from `init()` (a second call moments later already covers it), and the swipe-up-to-open trigger zone (bottom 1/6 of the screen) now subtracts the bottom safe-area inset from its reference height, since that padding (added for the PWA bezel fix, see MobileEnhancements below) is dead space that had shrunk the effective grabbable zone without the JS threshold accounting for it. Same root cause also clipped the bottom bar's content (including the always-visible current-LoRAs strip): `reapplyPositions()` sizes the top section and bottom bar off raw `100vh`/`49vh` with no bottom-inset subtraction, so their normal-flow heights summed to slightly more than the actually-visible content box, and `body`'s `overflow: hidden` silently clipped the excess off the bottom bar. Fixed by subtracting the same live-read safe-area-bottom padding from every `100vh`/`49vh`-based height calc in that function. Re-check this area after upstream merges in case upstream reworks the same gesture. **Perf pass (mobile typing jank):** added `scheduleReapply()` — a `requestAnimationFrame`-coalesced wrapper around `reapplyPositions()` — and routed the two prompt-textarea `input` handlers and the window `resize` handler through it, so a burst of keystrokes or iOS's continuous visualViewport keyboard-resize events collapse to one layout pass per frame instead of one synchronous full relayout per event. Also wrapped the `eval()`-based advance-width computation (and its `getComputedStyle`) in a `!isSmallWindow` guard, since its only consumer (the `<400px` min-width nudge) is already mobile-excluded — so on a phone that dead computation no longer runs on every pass. Desktop behavior is unchanged except that the same-frame calls are now batched. **Center-image-area floor:** `currentImageWrapbox`/`editorSizebar` height is computed as available space minus `altHeight` (the floating prompt bar's own live height, i.e. text boxes plus any pasted images); wrapped both calcs in `max(6rem, ...)` so that when `altHeight` grows larger than the space actually left over (easy on a short mobile viewport, especially with several pasted images — see the matching `mobile.css` cap above), the image/batch preview area — and its "Image"/"Batch" tabs — floors out at 6rem instead of collapsing to 0 while the ancestor's `overflow:hidden` simultaneously clips the prompt textboxes/Generate button out of view. Coupling watchlist: `reapplyPositions` internals + the input/resize call sites. **Relocating this file out of core was evaluated and rejected (2026-07) — do not re-propose it.** The swipe edits *can* be relocated cleanly (extension `ScriptFiles` are injected at `Text2Image.cshtml:120`, after `layout.js` constructs `genTabLayout` at `:110` and before `finalscript.js` at `:121` schedules `genpageLoad()` → `init()`, so an `addEventListener` shim around `GenTabLayout.prototype.init` can wrap both handlers). The `reapplyPositions()` edits cannot: they are threaded through ~130 lines of upstream layout math, so relocating means copying that math into the extension, where an upstream change to it diverges *silently* — whereas an in-place core edit raises a visible merge conflict. `layout.js` stays a modified core file either way, so the fork's core-edit count does not drop; the only gain would be a smaller diff, which does not justify trading a loud failure mode for a quiet one.
- `src/wwwroot/js/genpage/gentab/params.js`, `src/wwwroot/js/genpage/gentab/presets.js`, `src/wwwroot/js/genpage/main.js` — **core-file edits** (low risk, additive). Initial page load no longer builds the "New Preset" modal's duplicate parameter-panel DOM (`genInputs(false, false)` in `main.js`) — it's built lazily on first use of that modal (`ensurePresetInputsBuilt()` in `presets.js`). Halves the parameter-DOM-build/select2-init work on every load for sessions that never open that modal; every other `genInputs()` caller is unaffected (parameter default preserved).
- `src/wwwroot/js/genpage/helpers/browsers.js`, `src/wwwroot/js/genpage/helpers/ui_improvements.js` — **core-file edits** (low risk, matches an existing pattern used elsewhere in the same files). On iOS Safari, `position: fixed` popovers (the browser thumbnail-card action menu, and the thumbnail-backed Model/VAE/LoRA dropdown replacement) were appended as children of panels that have `-webkit-overflow-scrolling: touch` (`.input-sidebar`, `.browser_container`, added for momentum scrolling in the MobileEnhancements extension). That CSS property makes WebKit create a new containing block, which traps `position: fixed` descendants inside it instead of letting them float over the whole page — the popup rendered clipped/covered instead of on top. Fixed by appending both popovers to `document.body` instead, matching how other `AdvancedPopover` call sites in the same files already do it (and matching that class's own documented recommended usage). That fix introduced a follow-on bug in `browsers.js`: the thumbnail-card popover's ID is index-based (`${browserId}-${index}`), and it used to get destroyed for free whenever the browser list rebuilt (`contentDiv.innerHTML = ''` on every navigate/refresh) since it was a child of that container; once moved to `document.body` it survives rebuilds, so a later render reusing the same index left a stale popover (bound via closures to the old file at that position) sitting in the DOM ahead of the fresh one — `getElementById` resolves to whichever exists first, so opening a card's menu after any list refresh could show/act on the wrong LoRA, preset, or model. Fixed by removing any existing element with that ID immediately before creating the new one.
  - `ui_improvements.js` also gained a second, separate fix in `AdvancedPopover.reposition()` (autocomplete-visibility pass, 2026-07): the above/below flip and the clamp math were anchored to the full layout viewport (`window.innerHeight`, floor `0`). Once the mobile keyboard-handling fix kept the floating prompt bar in view during typing (lifted above the keyboard via `--kb-inset`) instead of letting it shift off-screen, the prompt autocomplete popover started flipping *above* the prompt box and stranding itself off the top of the screen: on an iOS standalone PWA the keyboard shrinks the visual viewport and scrolls the layout viewport up (`visualViewport.offsetTop > 0`) while `window.innerHeight` stays full, so clamping the flip-above `y` to layout-`0` put the popover above the visible band. Fixed by computing the visible band from `window.visualViewport` (`[offsetTop, offsetTop + height]`) and using it for the below-fits test, the flip midpoint, the flip-above floor, and the below-branch `maxHeight` — `getBoundingClientRect` (source of `targetY`) and `position:fixed` are both layout-viewport-relative, so the band is expressed in those same coordinates. Desktop is unchanged (`offsetTop` 0, `height` == `innerHeight`). Popover is recreated per keystroke, so no viewport-change listener is needed. Coupling watchlist: this shares the `--kb-inset` keyboard-lift behavior in `mobile_core.js`/`mobile.css` — re-check together if either changes.
