# C# and related tools
bin/
obj/
.vs/
.vscode/
.idea/
*.user
*.suo
Properties/
.dotnet/
.aspnet/
.local/

# So you can customize build if you need to
Directory.Build.props

# This project
/Output
/Data
/Models
tmp/
/src/Extensions
/dlbackend
*.debug
custom-launch-docker.sh
dotnet-install.sh
docker-compose.yml
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Python
__pycache__/
venv/
*.pth

# Node / Playwright verify tooling (opt-in, deliberately NOT a repo dependency - see AGENTS.md).
# Installed locally to run the verify/*.mjs harnesses; never commit the install artifacts.
node_modules/
package.json
package-lock.json

# Local backup tooling (not part of upstream)
/SwarmUI-Backup.ps1
/SwarmUI-Backup.bat

# Private local notes (contains LAN/Tailscale addresses - never push)
/Hub-Spoke-Setup.md
