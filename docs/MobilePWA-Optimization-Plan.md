# Mobile/PWA Optimization Plan (Fork)

Implementation handoff document for making SwarmUI feel robust, fluid, and intuitive on mobile and as an installed PWA, on this fork (socrasteeze/SwarmUI). Written so a coding agent (or human) can implement it phase-by-phase without re-doing the research.

Read `AGENTS.md` first. All work follows the fork's merge-friendly policy. Extension code remains the default; the small core API additions used by `/simple` are called out in the coupling watchlist.

## Implementation status (living)

Phases 1–4 are **implemented** in `src/BuiltinExtensions/MobileEnhancements/`. What shipped vs. what was intentionally deferred/changed:

- **Phase 1 (PWA scaffold + service worker)** — done. `MobileEnhancementsExtension.cs` maps root `/manifest.json` + `/sw.js` and injects head tags in `OnPreLaunch`; `sw.js` (network-first for unfingerprinted HTML/JS/CSS, **cache-first for any `?vary=`-fingerprinted URL** as of 2026-08-28, cache-first static, a 50-entry `/View/*?preview=true` thumbnail runtime cache, offline fallback), `mobile_core.js` (viewport fix, SW registration, standalone/coarse-pointer flags, keyboard handling), `mobile.css`, and icons generated from `favicon.ico`.
- **Phase 2 (image viewer)** — done. `mobile_fullview_touch.js`: pinch/pan/double-tap zoom, horizontal swipe navigation driving the core `shiftToNextImagePreview`, swipe-down dismiss, tap toggles metadata. **Deferred within Phase 2**: the full custom action-overlay chrome and in-viewer Share button (§2c) — instead, tap-on-image toggles the existing metadata/undertext (which already holds the per-image action buttons), and tap-outside closes. Share via `navigator.share` — **shipped 2026-08-28**: a coarse-pointer-only Share button in the full-view modal (`fetch` -> `File` -> `canShare`/`share({files})`, falling back to `share({url})`). Adjacent (+/-1) image preload (§2b) also shipped in the same pass. `ImageCompareHelper` touch parity — shipped: the same file's `MobileImageCompareTouch` class adds one-finger pan, pinch zoom (anchored, replicating `onWheel`'s math), and one-finger slide-divider drag on `#image_compare_stage`, driven entirely through `imageCompareHelper`'s own public methods/fields so touch and mouse share one viewport state.
- **Phase 3 (layout & ergonomics)** — mostly done: 44px touch targets, horizontally-scrollable tab strips, pull-to-refresh suppression, momentum scroll, visualViewport keyboard-avoidance, PWA safe-area padding. §3.3 (sidebar swipe animation) **dropped 2026-08-29** — upstream now ships it itself (follow-finger panel drag with CSS transitions and a `prefers-reduced-motion` opt-out in `layout.js`/`genpage.css`); see §3.3 below. §3.6 (dvh/safe-area overrides) and §3.7 (history thumbnail ergonomics) shipped 2026-08-29.
- **Phase 4 (network resilience)** — done, with one deliberate design change: instead of **wrapping** the core `genericRequest`/`makeWSRequest` (fragile, and unverifiable without a running server), the connection banner is driven by the browser `online`/`offline` events (robust, decoupled, covers the primary mobile network-drop case). Haptics (batch MutationObserver) and screen wake-lock during generation shipped as planned. Server-reachable-but-app-down detection **shipped 2026-08-29** without request wrapping: `mobile_network.js` wraps `site.js`'s `genericServerError` (the default transport-failure handler, hit by the status loop within seconds of a crash); when `navigator.onLine` it shows "SwarmUI isn't running — retrying…" and re-probes `API/GetCurrentStatus` every 5s (also on `visibilitychange`/`online`). `offline.html` does the same via a HEAD probe and reloads when the server answers. Haptics preference also unified on `m_client_haptics` (the `/simple` More-tab toggle now governs the genpage too). Item 4 (thumbnail runtime cache) **shipped 2026-08-28**.
- **Phase 5** — item 2 (manifest `shortcuts`) and item 3 (first-run standalone toast, **shipped 2026-08-28**) done; item 1 (Simple tab as mobile home) not started.
- **Civitai share-to-download** — done. `manifest.json` declares a `share_target` (GET, params `url`/`text`/`title`); `MobileEnhancementsExtension.cs` maps a root `/ShareTarget` route (same pattern as `/manifest.json`) that pulls a Civitai URL out of the shared fields and redirects to `/Text2Image#downloadmodel=<encoded url>`; `mobile_share.js` (new `ScriptFiles` entry) reads that hash flag on session-ready, opens the Utilities > Model Downloader tab, and prefills + `triggerChangeFor`s the URL field so Civitai metadata auto-loads. Fail safe: a non-Civitai (or absent) link lands on the downloader empty. This couples to the downloader's DOM IDs — see the watchlist.
- **Native app shell — REMOVED (2026-08).** The fork's bottom tab bar + parameters drawer are gone; the genpage runs upstream's own native mobile layout (shipped `b8c9718`..`23baf7d`, merged at `f725238`). Running both at once was the cause of the reported clipping and gesture weirdness. Code preserved on branch `fork/mobile-shell-legacy`; full rationale in `AGENTS.md`'s Fork Delta. Do not re-add a second mobile layout system to the genpage — extend upstream's, or use `/simple`.
- **Mobile perf** — done, and independent of the shell. `layout.js` gained `scheduleReapply()` (rAF-coalesced) on the prompt-input/resize handlers, and the `eval()` advance-width pass is skipped on `small-window` (dead there) — kills the per-keystroke relayout jank.
  **Second pass (2026-08).** The per-*frame* mobile hot paths (topbar collapse, panel drag, the document-wide lazy-image scan) now read a cache filled once per layout pass instead of measuring the DOM on every scroll/touchmove event; `browsers.js`'s own scroll listener got the same treatment. Same session: the installed PWA's missing top safe-area padding is fixed (`--safe-top`, `mobile_core.js`), and a large model library's page-load cost is fixed at the source (`makeMultiselectInput`'s lazy-option-list branch, `site.js`/`params.js`) rather than papered over. A new discrete top-of-page loading bar (`busy_indicator.js`) covers the one freeze that fix relocates rather than removes — the first open of a huge dropdown. Full detail, including measurements, is in `AGENTS.md`'s Fork Delta; `verify-mobile-perf.mjs` covers all of it (45 checks).
- **`/simple` startup and power controls (2026-08)** — done. Steps and CFG use paired 44px steppers. `ListT2IParams(compact: true)` omits the duplicate LoRA parameter-values array while keeping `models.LoRA`; autocomplete data is excluded from the boot `GetMyUserData` response and fetched on first prompt focus through an async cached loader with timeout/retry. The loader uses atomic reload generations, single-flight formatting, request-abort cancellation, cached limit failures, and per-user throttling. The mobile ceiling is 32 MiB of source text, 500,000 entries, 32 million formatted characters, and a 256-character suffix; a larger list warns once without disabling prompt-syntax or TagDex completion. If the configured source is `character_tags`, `/simple` automatically uses an exact same-directory `all_tags` source while TagDex continues to own `<character:`. State-driven redraws are coalesced to one animation-frame callback. The rich LoRA picker builds an O(1) name map, caches its lowercase search corpus, and uses a `Set` for active-name exclusion.
- **`/simple` compact control pass (2026-08-29)** — done. Seed starts as Random and expands to an editable explicit value with an × escape back to `-1`; aspect and side length are one final-resolution picker whose options name the actual output dimensions; Architecture and Presets share one row with the all-architecture label shortened to All; LoRA weights use exact 0.05 minus/input/plus controls; and TagDex adds a Characters browse sheet with dataset search and tap-to-insert. `verify-simple-create-panel.mjs` covers these contracts and the genpage delete-current fallback (62 checks; 237/237 across all seven mobile harnesses in the same pass). The restarted Release server confirmed the controls, TagDex's first 50 live results and prompt insertion, and LoRA increment/removal at a 430px viewport. Physical-device checks remain separate.

- **`/simple` error surface, preview canvas, and starred models (2026-08-16)** — done. Errors render in a strip inside the header (taking the title's space) instead of site.js's `center_toast`, which on a phone covered the header, prompt box and model strip and never auto-dismissed; the header keeps its height, so nothing below it moves when an error arrives, expands or clears. The preview block's height is reserved from first paint (`--m-preview-h`), identical idle/queued/generating/done, so a generation no longer pushes the model row, Generate and the prompt box down the screen; cells size from the row count so 1-up, 2-up and 2×2 all fill the same canvas, and the collapse toggle is still how that space is reclaimed. The LoRA and checkpoint pickers and the Models tab sort starred models first with a `★`, read from the `starred_models` the boot `GetMyUserData` already returns — sorted *before* the 120-row render cap, which is what decides whether a favourite is on screen at all. Full detail in `AGENTS.md`'s Fork Delta; `verify-simple-error-banner.mjs` (22 checks) and `verify-simple-create-panel.mjs` (23 checks) cover them, both known non-vacuous against the pre-change tree.
- **`/simple` Restart server row (2026-08-16)** — done. **More → Restart server**, not to be confused with the "Force update" row beside it (that one is client-side only: caches, service worker, reload). This one calls `UpdateAndRestart {force: true}` — the same call the genpage's extension restart button makes, git-updating nothing — so the Release-build asset cache described at the top of `AGENTS.md` can finally be cleared from a phone. The watcher behind it waits for the server to go *down* before waiting for it to come back, because the server stays responsive for a moment after accepting the request and a naive poll reloads into the old process. Full detail in `AGENTS.md`'s Fork Delta; `verify-simple-restart.mjs` (20 checks) covers it.
- **`/simple` prompt-image crop/rotate editor (2026-08-16)** — done. A new file, `m_image_edit.js`, opened by tapping (not long-pressing — see the watchlist row below) a thumbnail in the image strip: rotate in fixed 90° steps, a draggable axis-aligned crop rectangle, Undo, Save, Cancel and a "Remove image" button, all in a bottom sheet. Everything is throwaway until Save; a server-referenced image is forked into a local data URI on save rather than mutating the server's file. The strip's own remove × shrank now that it's not the only way off an image. Full detail in `AGENTS.md`'s Fork Delta; `verify-simple-image-editor.mjs` (25 checks, including hand-dispatched touch events against the real drag listeners) covers it, known non-vacuous against the pre-change tree. Not device-verified.

**Verification owed (blocking real use):** repository policy forbids agent-run builds and tests. Static verification covers `node --check` on changed JS, `dotnet format --verify-no-changes --no-restore`, `git diff --check`, and cross-file contract tracing. Before relying on this, run the documented build/manual gate yourself and check the cold/warm installed PWA on a real phone.

## Why

**Historical.** This is the state found by the original audit (upstream-equivalent master, 2026-07). Every bullet below has since been addressed by Phases 1-4 above; it is kept as the record of why the work existed, not as a description of the current tree.

- **No PWA support at all** (then): no web manifest, no service worker, no `theme-color`/`apple-mobile-web-app-*` meta tags, only a 128×128 `favicon.ico`.
- **Pinch zoom is blocked globally** by `maximum-scale=1.0` in the viewport meta (`src/Pages/Shared/_Layout.cshtml:5`).
- **The fullscreen image viewer is mouse-only** (`ImageFullViewHelper` in `src/wwwroot/js/genpage/gentab/currentimagehandler.js`): wheel zoom + mouse pan, zero touch handlers.
- **Responsive layout is JS-driven, partial**: `body.small-window`/`large-window` classes from `js/genpage/gentab/layout.js` (auto = `innerWidth < 768`, or the User-tab Mobile/Desktop dropdown); only 2 `@media` queries in all CSS; sidebar swipe gestures exist. (This line used to cite a `layout.js:465` TODO, "Mobile bar shuts need a smooth animation" — **that TODO no longer exists**; upstream's mobile-layout rewrite removed it. The only TODO in `layout.js` today is the unrelated eval hack. Verified 2026-08-28.)
- **No network resilience**: no offline handling, no reconnect backoff, no connection status UI — painful on flaky mobile connections.

## Verified design facts (do not re-derive; re-verify only after large upstream merges)

1. **`src/Extensions/` is gitignored** (`.gitignore:22`) and excluded from compile (`SwarmUI.csproj`). Fork-tracked extension code must live in **`src/BuiltinExtensions/<Name>/`** — a plain folder with `.cs` files (no `.csproj`), auto-discovered by `ExtensionsManager.PrepExtensions()` (`src/Core/ExtensionsManager.cs`, enumerates `./src/BuiltinExtensions`), compiled into the main assembly. Structural template: `src/BuiltinExtensions/GridGenerator/`.
2. **Lifecycle order** in `src/Core/Program.cs`: `Web.Prep()` (line ~348) → `Extensions.RunOnAllExtensions(e => e.OnPreLaunch())` (line ~351) → `Web.Launch()` (line ~355). Therefore an extension's `OnPreLaunch()` can:
   - **Map root-scoped routes**: `WebServer.WebApp` is `public static WebApplication` (`WebServer.cs:25`) → `WebServer.WebApp.MapGet("/sw.js", ...)`. A root-scoped service worker needs no core edit and no `Service-Worker-Allowed` header.
   - **Inject `<head>` HTML on every page**: `WebServer.PageHeaderExtra` is `public static HtmlString` (`WebServer.cs:77`), populated during `Prep()` (`WebServer.cs:362`), rendered at `_Layout.cshtml:25`. Append via `WebServer.PageHeaderExtra = new(WebServer.PageHeaderExtra.Value + "...");`.
3. **Extension asset plumbing** (`src/Core/Extension.cs`): `ScriptFiles` (JS injected on the genpage after all core scripts, before `finalscript.js` — extension JS can rely on core globals), `StyleSheetFiles` (CSS in `<head>` on all pages), `OtherAssets` (static files served at `/ExtensionFile/<ExtName>/<path>`).
4. **JS hook arrays** extension scripts can push to: `sessionReadyCallbacks` (`js/genpage/main.js:13`), `featureSetChangedCallbacks` (`main.js:35`), `postParamBuildSteps` (`js/genpage/gentab/params.js:2`), `hideParamCallbacks` (`params.js:1228`).
5. **`ImageFullViewHelper`** (`currentimagehandler.js`, ~lines 3-377): global instance `imageFullView`; primitives `detachImg()`, `moveImg(dx,dy)`, `getImg()`; zoom math in `onWheel` (~lines 176-203); `noClose` flag suppresses the modal-close click handler. `ImageCompareHelper` (~line 1413) follows the same mouse-only pattern.
6. **Viewport meta** can be fixed by runtime JS mutation of the meta tag (browsers honor dynamic viewport changes) — no `_Layout.cshtml` edit needed.
7. **Networking**: `site.js` globals `genericRequest` (JSON POST) and `makeWSRequest` (WebSocket per long op) are plain global functions — wrappable by reassignment.
8. Line numbers above are anchors, not gospel — re-locate by symbol name if upstream shifts code.

## End-state fork delta

```
AGENTS.md                                      (done)
docs/MobilePWA-Optimization-Plan.md            (this file)
src/BuiltinExtensions/MobileEnhancements/**    (new directory — all phases below)
```

No existing file is modified; behavioral couplings are tracked in the watchlist at the bottom.

---

## Phase 1 — Extension scaffold + PWA installability + service worker

New directory:

```
src/BuiltinExtensions/MobileEnhancements/
├── MobileEnhancementsExtension.cs
├── README.md                      (what this is, how to replace icons, coupling notes)
└── Assets/
    ├── manifest.json
    ├── sw.js
    ├── offline.html
    ├── icons/  icon-192.png, icon-512.png, icon-maskable-512.png, apple-touch-icon-180.png
    ├── mobile_core.js
    └── mobile.css
```

**`MobileEnhancementsExtension.cs`** — namespace `SwarmUI.Builtin_MobileEnhancementsExtension`, `class MobileEnhancementsExtension : Extension` (model on `GridGeneratorExtension.cs`; `///` docs on all members, no `var`):

- `OnInit()`: register `ScriptFiles` (`Assets/mobile_core.js`), `StyleSheetFiles` (`Assets/mobile.css`), `OtherAssets` (icons, `offline.html`).
- `OnPreLaunch()`:
  - `WebServer.WebApp.MapGet("/manifest.json", ...)` → serve `{FilePath}Assets/manifest.json` as `application/manifest+json`.
  - `WebServer.WebApp.MapGet("/sw.js", ...)` → read `Assets/sw.js`, prepend `const SWARM_VARY = '<Utilities.VaryID>';` (so cache names roll on each server version), serve as `text/javascript` with `Cache-Control: no-cache`.
  - Append to `WebServer.PageHeaderExtra`: `<link rel="manifest" href="/manifest.json">`, `<meta name="theme-color" content="...">` (read the actual background color from `src/wwwroot/css/themes/modern_dark.css` during implementation), `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style` (black-translucent), `mobile-web-app-capable`, `<link rel="apple-touch-icon" href="/ExtensionFile/MobileEnhancements/Assets/icons/apple-touch-icon-180.png">`.

**`manifest.json`**: name/short_name "SwarmUI", `start_url: "/simple"` (superseded during implementation — /simple became the mobile home surface), `scope: "/"`, `display: "standalone"`, colors matching modern_dark, icons array including a `purpose: "maskable"` 512.

**Icons**: originally generated from `src/wwwroot/favicon.ico` (largest frame extracted with ImageMagick, upscaled to 192/512; maskable = glyph padded to ~80% safe zone on the theme background). **Regenerated and recompressed 2026-08-28** (449 KB → ~102 KB across all four); the maskable glyph must stay inside the *circular* 80% safe zone — see `AGENTS.md`'s Fork Delta. A higher-res source image is still a drop-in replacement later.

**`sw.js`** (~120 lines, no libraries, GET-only):

- Cache names suffixed with `SWARM_VARY`; `activate` deletes old-suffix caches.
- Never intercept: `/API/`, `/View/`, `/ViewSpecial/`, `/Output/`, `/Audio/` (pass through).
- Navigations: network-first; on failure serve precached `offline.html`.
- Unfingerprinted JS/CSS/HTML: **network-first with cache fallback** — never serve cache when the network succeeds; the cache only helps when fully offline.
- `?vary=`-fingerprinted URLs: **cache-first** (changed 2026-08-28; this bullet originally said network-first). The URL itself carries the version, so a server update changes the URL and can never be stuck behind a stale cache entry; a bounded static cache (`MAX_STATIC_ENTRIES`) keeps it from growing unbounded.
- `imgs/`, `fonts/`, extension icons: cache-first.
- No push, no background sync.

**`mobile_core.js`** — class-based singleton `mobileEnhancements` (per convention):

- On load, mutate the viewport meta to `width=device-width, initial-scale=1.0, viewport-fit=cover` (drops `maximum-scale=1.0` → restores Android pinch zoom; `viewport-fit=cover` enables iOS `env(safe-area-inset-*)`). Companion CSS: under `body.small-window`, inputs/textareas/selects in the param + prompt areas get `font-size: 16px` (prevents the iOS focus auto-zoom that `maximum-scale` was masking).
- Register the SW (`'serviceWorker' in navigator` guard).
- Add `body.pwa-standalone` class when `matchMedia('(display-mode: standalone)')` matches or `navigator.standalone` is set.

**`mobile.css` (phase-1 minimum)**: safe-area (`env(safe-area-inset-*)`) padding for the top tab bar / bottom bar under `body.pwa-standalone`; `100dvh` overrides scoped to `body.small-window` for the main containers (exact selectors from `genpage.css` during implementation; class selectors only).

## Phase 2 — Mobile image viewer overhaul (touch gestures + seamless swipe navigation)

Goal: the fullscreen image experience on mobile should feel like a native photo gallery — pinch/pan/double-tap zoom, horizontal swipe to move seamlessly between images, swipe-down to dismiss, and a clean tap-to-toggle action overlay.

New file `Assets/mobile_fullview_touch.js` (added to `ScriptFiles`; loads after `currentimagehandler.js`, so `imageFullView` exists). Approach: **augment the existing `ImageFullViewHelper` modal** (not a parallel viewer) with a mobile gesture + chrome layer, active only when `body.small-window` or `(pointer: coarse)`. Use additive `touchstart/touchmove/touchend/touchcancel` listeners (`{passive: false}`; touch events rather than pointer events, to avoid interfering with the existing mouse handlers — matches the `image_editor.js` precedent) on the fullview modal content.

### 2a — Core touch gestures

- **One-finger pan**: `imageFullView.detachImg()` + `moveImg(dx, dy)`; if cumulative movement > threshold, set `imageFullView.noClose = true` so a drag doesn't close the modal; a genuine tap still closes (preserves current UX until 2c replaces tap behavior on mobile).
- **Two-finger pinch**: implement `zoomAt(clientX, clientY, factor)` replicating the `onWheel` math (height-percent zoom, clamp to `Math.sqrt(w*h)*2`, `imageRendering='pixelated'` past threshold, midpoint-anchored `moveImg` correction); drive it from the ratio of successive inter-touch distances, plus pan by midpoint drift.
- **Double-tap**: toggle fit ↔ ~2.5× zoom at the tap point.
- `preventDefault()` only while the modal is open; respect the same `video-controls`/`audio-controls` parent guards as `onMouseDown`.
- Apply the same helpers to `ImageCompareHelper`'s stage; if its internals diverge too much, ship viewer-only first and compare-view as a follow-up commit.

### 2b — Seamless swipe navigation between images

- **At fit zoom (not zoomed in), horizontal swipe advances to the previous/next image** in the current context (batch results or history grid), with a fluid slide: during the swipe, translate the current image with the finger (CSS `transform`, no layout thrash); on release past a distance/velocity threshold, animate the outgoing image off-screen and the incoming one in (~200 ms ease-out); otherwise spring back.
- Image-list source: the same sequence the existing arrow-key navigation uses — find the keyboard prev/next handler in `currentimagehandler.js` during implementation and drive that exact code path (fall back to reading sibling image elements of the batch/history DOM only if no reusable function exists). Never build a separate gallery state that can drift from core.
- Preload the adjacent (±1) full images when the viewer opens or navigates, so swipes land instantly on slow connections.
- When zoomed in, horizontal pan takes priority; swipe-navigation triggers only from fit zoom (edge-fling-while-zoomed is optional later polish).
- Rubber-band resistance at the first/last image so the boundary is felt.

### 2c — Mobile viewer chrome & dismiss gestures

- **Swipe-down to dismiss**: vertical drag translates the image and fades the backdrop; past threshold, close the modal (respecting `noClose` semantics); otherwise spring back.
- **Tap toggles a minimal action overlay** instead of instantly closing (mobile only; desktop click-to-close unchanged): top bar with close button + image index ("3 / 8"); bottom bar exposing the existing per-image actions by triggering the same handlers core uses (star, reuse parameters, delete, download — plus Share via `navigator.share({files})`, feature-guarded). Overlay auto-hides after a few seconds or on next tap; all buttons ≥ 44px.
- Light haptic tick (`navigator.vibrate?.(10)`, feature-guarded; shared toggle with Phase 4) on image change and dismiss.
- Chrome CSS lives in `mobile.css`, scoped under `body.small-window`; zero changes to desktop viewer visuals.

## Phase 3 — Layout & ergonomics (grow `mobile.css` + `mobile_core.js`)

All scoped under `body.small-window` and/or `@media (pointer: coarse)` — composes with every theme, desktop untouched. Prioritized:

1. **Touch targets ≥ 44px**: top nav-tabs, bottom-bar sub-tab links, sidebar toggles, param group headers, batch-item buttons. Pure CSS.
2. **Tab overflow on narrow screens**: horizontal scroll for the top tab bar + bottom sub-tab list (`overflow-x: auto; flex-wrap: nowrap; -webkit-overflow-scrolling: touch;`, hidden scrollbar, `scroll-snap-type: x proximity`). Pure CSS.
3. **Sidebar swipe animation polish** — **DROPPED 2026-08-29; superseded by upstream.** It was written against a `layout.js:465` TODO ("Mobile bar shuts need a smooth animation") that upstream removed in its mobile-layout rewrite. Re-evaluated against the current tree: upstream's `layout.js` now implements follow-finger panel drags (`getMobileGestureForTouch` / `finishMobileDrag`, `openMobilePanel` / `closeMobilePanel`), and upstream's `genpage.css` transitions `#input_sidebar`, `#current_image_batch_wrapper`, `#t2i_bottom_bar` and the scrim, suspending them only while `.mobile-panel-dragging` and under `prefers-reduced-motion`. That is the animation this item asked for. Nothing to add on the fork side; do not layer a second animation on top.
4. **Prompt/Generate ergonomics with the keyboard open**: add `interactive-widget=resizes-content` to the (JS-mutated) viewport meta for Android; a `visualViewport` resize listener offsets the floating `alt_prompt_region` while the prompt textarea is focused (iOS); enlarge the Generate button hit area on mobile.
5. **Pull-to-refresh suppression**: `body.small-window { overscroll-behavior-y: none; }` (prevents accidental reload mid-generation while swiping the bottom bar open).
6. **`dvh`/safe-area completion** — **shipped 2026-08-29**: `mobile.css` overrides the remaining `genpage.css` `100vh` rules that reach a phone (fullview imagewrap under `coarse-pointer`, the compare modal with `--safe-bottom`, the Simple tab's three `calc(100vh - …)` rules) with `100vh` → `100dvh` pairs; genpage.css untouched. Deliberately left: `body.small-window { height: 100svh }` (core's choice — `dvh` there causes URL-bar relayout jank) and the id-scoped `#image_fullview_modal` rule (cannot be overridden without an id selector).
7. **History/batch browsing** — **resolved 2026-08-29**. The `?preview=true` + lazy-load half turned out to already be core behaviour (`outputhistory.js` emits `?preview=true`; `browsers.js` scroll-lazyloads via `.lazyload`/`dataset.src`), so no post-processing hook was added. Tiles were already ≥7rem; the genuinely undersized target was the per-tile ☰ menu (~1em, hover-revealed), now 44×44 and always visible under `body.coarse-pointer`. Separately, `Assets/mobile_thumbnails.js` reassigns the fork's `getThumbnailSrc` so `Output/…` srcs (`AppendUserNameToOutputPath = false`) also get the batch-strip 256px preview.

## Phase 4 — Network resilience & polish

New file `Assets/mobile_network.js` (+ small CSS):

1. **Connection status + reconnect**: listen to `online`/`offline`; wrap the globals `genericRequest`/`makeWSRequest` by reassignment (capture the original in a `let`) to count consecutive failures; show a persistent "Connection lost — retrying" banner (reuse existing toast/status-bar infrastructure); exponential-backoff reconnect probing; clear on first success. Do not alter core retry semantics.
2. **Haptics-lite**: `navigator.vibrate?.(10)` on generation complete and sidebar swipe, behind a localStorage toggle (default on for coarse pointers; iOS safely no-ops). Hook completion via a MutationObserver on the batch container (avoids touching core).
3. **Screen wake-lock during generation** (toggleable): `navigator.wakeLock.request('screen')` while a generation WS is active; release on completion/visibility change. Phones sleeping mid-generation is a real mobile pain point.
4. **(Optional, only if ≲40 extra sw.js lines)**: runtime-cache the last ~50 `/View/*?preview=true` thumbnails (cache-first + revalidate, LRU-capped) so the history grid skeleton renders offline.

## Phase 5 — Optional enhancements (each independent; pick freely)

1. **Simple tab as mobile home** (opt-in localStorage toggle): when `pwa-standalone` + `small-window`, auto-switch to the Simple tab on launch via `sessionReadyCallbacks` — it's already the touch-friendliest generation surface.
2. ~~**Manifest `shortcuts`** for Generate/Simple.~~ — **shipped**; `manifest.json` carries a `shortcuts` array with a Generate entry pointing at `/simple`.
3. **First-run standalone toast** suggesting the User-tab Mobile/Desktop dropdown setting (never silently change user settings).
4. **Considered and rejected**: deduplicating the duplicate `isSmallWindow` logic (`browsers.js` vs `layout.js`) — a core-file refactor with zero user-visible gain; merge-hostile.

---

## Sequencing & verification gate

One commit (or PR) per phase: 1 → 2 → 3 → 4 (→ 5). Phases 2–4 are independent of each other once Phase 1's scaffold lands.

**Gate for every phase:**

1. `dotnet build SwarmUI.sln --configuration Release` passes.
2. `dotnet format SwarmUI.sln --verify-no-changes` clean (CI-enforced).
3. `./launch-linux.sh --ci-test true --launch_mode none --loglevel debug` boots clean; log shows the MobileEnhancements extension discovered.
4. Phase 1+ manual: `curl -sI localhost:7801/sw.js` → 200 `text/javascript`; `/manifest.json` → 200; page head shows the injected tags. Chrome DevTools → Application → Manifest: installable, no errors; Lighthouse PWA audit passes; DevTools offline toggle → offline.html; back online → fresh JS (no staleness).
5. Phase 2+: real device or DevTools touch emulation — pinch anchors under fingers, drag doesn't accidentally dismiss, swipe left/right at fit zoom moves between images fluidly (with preload — no blank flash), rubber-band at the ends, swipe-down dismisses, tap toggles the action overlay, desktop mouse behavior unchanged.
6. Desktop no-regression pass (large-window UI visually unchanged) every phase.
7. After any future upstream merge: re-run this gate + re-check the coupling watchlist.

## The bottom-nav gap in the installed iOS app (2026-08-23, fix shipped 2026-08-29)

On the target iPhone (iOS 18.7 / WebKit, 956pt screen) the installed app rendered a band of reserved space
below `.m-bottom-nav`, so the bar did not touch the physical screen edge. First logged as "accepted device
behaviour"; **re-read on 2026-08-29 the diag numbers below identify the cause, and a fix shipped** — see
"Resolution" at the end of this section. The measurements are kept because they are the evidence.

Measured on device with `Assets/m/_diag.html` (under status-bar-style `black-translucent`, the value shipped
at the time):

| | |
|---|---|
| `innerHeight` / `screen.height` | 894 / 956 — the web view is **62px short** |
| `100dvh` and `height: 100%` | **both** resolve to 894px |
| all four `env(safe-area-inset-*)` | **0px**, top included |
| `display-mode: standalone` / `navigator.standalone` | `false` / `true` |

What that rules out, each verified rather than assumed:

- **Not a CSS height-unit problem.** `100dvh` and `height: 100%` measured identical, so no swap between
  them can help. A `body.pwa-standalone { height: 100% }` rule was shipped on this premise and reverted
  the same day once the device data came back.
- **Not paintable by the page.** An `html` background colour did not reach the band — it is outside the
  page canvas entirely.
- **Not `theme-color`.** Setting it to a distinct colour did not reach the band either.
- **Not a stale or legacy install.** A fresh install from the HTTPS origin, with the manifest and
  `display: standalone`, still shows it.
- **Not `detectStandalone()` failing.** It falls through to `navigator.standalone`, which the device
  reports true, so `body.pwa-standalone` *is* applied and standalone-scoped rules are live.

The root condition is that `viewport-fit=cover` is not taking effect — every inset reports 0 and the view
is letterboxed — despite the meta being present in `index.html`, re-applied by `mobile_core.js`'s
`fixViewport()`, and `apple-mobile-web-app-status-bar-style: black-translucent` being injected. The bar is
correctly positioned at the bottom of the viewport it is given; the viewport is simply not the screen.

Second consequence, found on-device 2026-08-28 on the genpage: the 62px shortfall is a band at the
BOTTOM while the top still renders under the clock, so `measureSafeAreaTop()`'s old "window shorter than
screen means iOS reserved the top" guard misfired and returned 0 - the tab strip sat behind the status bar.
The guard now discriminates with `window.screenY` (a top-reserved webview sits at screenY >= the bar height;
a full-bleed one at 0), pinned by two cases in `verify-mobile-perf.mjs` including this exact device shape.

Consequence worth knowing: with the bottom inset reporting 0, `--m-safe-bottom` computes to 0, so
`.m-bottom-nav`'s `padding-bottom` collapses. That padding is doing nothing on this device — the clearance
comes from the reserved band instead. If a future iOS starts reporting the inset properly, the padding
resumes and no change is needed.

**Resolution (2026-08-29).** The shortfall is not an arbitrary number: 62px is exactly this device's
status-bar height (`IosStatusBarHeights[956]`). So iOS was sizing the web view as if the status bar were
*reserved* (the `black`/`default` geometry, 956 − 62) while positioning it as if *translucent* (full-bleed,
y=0). The reserved 62px had nowhere to go but the bottom, outside the page canvas — hence unpaintable by
`html` background or `theme-color`. The fix asks for the geometry iOS was already computing:
`MobileEnhancementsExtension.StatusBarStyle` is now `black`, so the same 894px view starts below the clock
and ends at the screen edge — no band, no content under the status bar, and `--safe-top` legitimately 0.
`measureSafeAreaTop()` reads the injected meta and returns 0 unless it is `black-translucent`, so the
fallback table survives for anyone who flips the constant back; `verify-mobile-perf.mjs` pins both (50/50).
Cost: an opaque black status-bar strip instead of `#161616`. **Not yet device-verified** — delete and re-add
the home-screen app so iOS re-reads the meta, then open `_diag.html` (it now prints the meta value) and
check `screen.height - innerHeight` is still 62 but the band is gone. If it is not, this diagnosis is wrong.

## Coupling watchlist (re-check after every upstream merge)

Re-verified 2026-09-01 after upstream merge `b24d62b3` (site.js, prompttools.js, T2IPromptHandling.cs, LegacyPromptParser.cs).

These fork files depend on unedited core internals. Upstream refactors won't cause git conflicts — they cause silent breakage, so check these by hand:

| Fork file | Coupled to (unedited core) | Re-check |
|---|---|---|
| `mobile_fullview_touch.js` | `ImageFullViewHelper` method names/zoom math + arrow-key prev/next navigation path in `currentimagehandler.js` | method signatures, `noClose` flag, nav handler |
| `mobile_core.js` (anim hook, viewport mutation) | `layout.js` `reapplyPositions()` + bar classes; `_Layout.cshtml` viewport meta | swipe/gesture region; meta tag shape |
| `mobile_network.js` | `site.js` global `genericServerError` (the default failure callback of `genericRequest`/`makeWSRequest`); `API/GetCurrentStatus` answering any POST | the server-down banner still fires on a crash and clears when the probe gets an HTTP response. **2026-09-01:** `b24d62b3`'s site.js diff was entirely inside `textPromptAddKeydownHandler` (the new `<weight[...]:>` keyboard shortcut) — `genericServerError` (site.js:108) is untouched. Booted a throwaway server; `.mobile-connection-banner` was never created (no spurious fire) on a clean `/Text2Image` or `/simple` load. No fix needed |
| `mobile_thumbnails.js` | the fork's `getThumbnailSrc` global in `currentimagehandler.js` being resolved by name at call time; `WebServer.ViewOutput` honouring `?preview=true` for both `View/` and `Output/` | `Output/…` srcs still get the 256px preview; if core renames the helper this override silently becomes a no-op |
| `mobile_share.js` | Model Downloader DOM: element IDs `utilitiestabbutton`, `modeldownloadertabbutton`, `model_downloader_url` (`Text2Image.cshtml` / `UtilitiesTab.cshtml`); `modelDownloader` global + its `oninput`/`urlInput` wiring; `triggerChangeFor`/`sessionReadyCallbacks` globals | tab-button + URL-field IDs unchanged; input event still fetches Civitai metadata |
| `layout.js` perf (`scheduleReapply`, mobile eval guard) | `reapplyPositions()` internals + the prompt-input / window-resize call sites | rAF-coalescing still correct; the `<400px` width-nudge still `!isSmallWindow`-only |
| `layout.js` `getViewportHeight()` ↔ `body` padding-bottom | `getViewportHeight()` subtracts `body`'s computed `padding-bottom` (the installed-PWA safe-area inset from `mobile.css`'s `body.pwa-standalone`), and all six callers depend on that single definition: `reapplyPositions`, `reapplyMobilePositions` (via `viewH`), `setMobileTopbarCollapse`, `openMobilePanel`, `applyMobileDragTransform`, `getMobileGestureForTouch`, and the bottom-edge swipe test | **This row exists because of a real silent failure — read it before "simplifying" any of it.** The fork originally put this correction inside `reapplyPositions()` when that was one function; upstream's `b8c9718` split it into `reapplyMobilePositions()`/`reapplyDesktopPositions()` and the correction was orphaned onto the desktop half, **with no merge conflict**, leaving mobile sizing to the full window for weeks. After any upstream merge: confirm the mobile path still reaches a padding-aware height, and confirm no call site has grown its own second `paddingBottom` subtraction (that double-counts — the bottom-edge swipe zone had one and it was removed). **The mobile and desktop layout paths are separate functions; fixing one does not fix the other, and git will not tell you.** |
| `mobile_core.js` `measureSafeAreaTop()` → `--safe-top` | `body.pwa-standalone { padding-top: var(--safe-top, env(safe-area-inset-top)) }` in `mobile.css`; the `pwa-standalone` class still being applied; `layout.js` deriving its mobile heights from the root div's live top offset (so the padding self-corrects) rather than from `innerHeight` directly | **iOS reports a top safe-area inset of 0 for home-screen web apps** even under `black-translucent`, which is why the raw `env()` left the top tab strip under the status bar. Do not "simplify" this back to a bare `env()`. The fallback is gated on standalone + iOS + portrait + a proven-full-bleed window (`screen.height - innerHeight < 20`); dropping any gate produces a permanent dead band somewhere (Android portrait PWAs, landscape, or a window iOS already shortened). If upstream/WebKit ever starts reporting the top inset correctly, the reported value already wins automatically — no change needed. The status-bar-height table is only reached when the browser reports nothing, and errs high for unrecognized hardware on purpose. **Since 2026-08-29 the whole fallback is inert in production**: the extension injects status-bar-style `black` (`MobileEnhancementsExtension.StatusBarStyle`) and `measureSafeAreaTop()` returns 0 for anything but `black-translucent`. Keep the meta and that check in step |
| `layout.js` `mobileMetrics` cache (`refreshMobileMetrics`) | `setMobileTopbarCollapse` and `applyMobileDragTransform` read cached measurements instead of measuring; the cache is invalidated at the top of `reapplyPositions()` and refilled by the pass. **`rootTopBase - collapsePx` replaces a live `getBoundingClientRect()`**, and that identity holds only because `genpage.css`'s `body.small-window > #toptablist` applies the collapse as a `translateY` *plus an equal negative `margin-bottom`* | If upstream changes how the collapse is applied — drops the negative margin, switches to `height`, adds a transition — the arithmetic silently diverges from the real offset and mobile panels size wrongly with no error. `verify-mobile-perf.mjs` sweeps 41 collapse values against the real CSS and is the cheap way to check this. Also: anything new that changes `#alt_prompt_region`'s height, the bottom peek, or `body`'s padding **must** route through `reapplyPositions()`, or the cache goes stale |
| `GenTabModals.cshtml` paste box (`contenteditable`) | The `image_editor_paste_modal` body: the box is a `contenteditable` div with `data-placeholder`, not an `<input>`, and its `<style>` block lives in the same file. `handlePasteModalPaste` reads an `<img>` back out of it; `openPasteModal` clears it with `innerHTML` | If upstream reworks this modal, keep the div — reverting to an `<input>` silently makes the modal desktop-only again with no error. Check `--text-soft`/`--border-color`/`--background`/`--text` still exist in the themes |
| `image_editor.js` clipboard rejection routing | `pasteSelectionFromClipboard`'s `navigator.clipboard.read()` promise shape; the `image_editor_paste_modal` / `image_editor_paste_pastebox` element IDs in `GenTabModals.cshtml`; `WebUtil.ModalHeader` still emitting a **non-`fade`** modal | The fork moved upstream's four modal-opening lines into a new `openPasteModal()` — an upstream edit inside the old `if (!navigator.clipboard ...)` block conflicts against a function that no longer has them; apply it inside `openPasteModal()`. If `ModalHeader` gains `fade`, the trailing synchronous `box.focus()` stops landing and needs a `shown.bs.modal` re-focus |
| `MobileEnhancementsExtension.cs` | `WebServer.WebApp`, `PageHeaderExtra`, `Extension` asset lists; `/ShareTarget` route reachability | `Prep()` → `OnPreLaunch()` ordering in `Program.cs` |
| `MobileEnhancementsExtension.cs` (theme registration) | `WebServer.RegisterTheme` signature + the `ThemeData(ID, Name, CSSPaths, IsDark)` record shape; `RegisteredThemes.Clear()` still living in `PreInit()` (i.e. before `OnInit`); the `modern.css` (structure/derived vars) vs `modern_dark.css` (raw palette) split; `Extension.ExtensionName` still being the class name, so `/ExtensionFile/MobileEnhancementsExtension/…` resolves | theme still appears in User Settings and loads all three sheets in order; a palette regression (unstyled/black UI) means `modern_dark.css` fell out of `CSSPaths` |
| `theme_mobile.css` (sticky hover) | Hardcoded resting values copied from core: `.basic-button` (`site.css:557`), `.generate-button`/`.interrupt-button`/`.refresh-button` (`genpage.css:16/44/567`), `#alt_generate_button` pair (`modern.css:382-397`, ID-specificity — needs `!important`), `.auto-input-qbutton` (`site.css:424`), `.text_button` (`site.css:596`), `.nav-tabs .nav-link` (`modern.css:109-123`), `.image-block` (`genpage.css:313` + `modern.css:497-505`), `.model-block img` (`genpage.css:400`), `.model-block-menu-button` (`genpage.css:457`), `.sui_popover_model_button` (`genpage.css:502`), browser list trio (`genpage.css:1160/1189/1234`), `.auto-symbol` (`modern.css:239-269`), `.top-status-bar-wrapper` (`site.css:78`), `.batch-gear-button` (`genpage.css:237`), `.button-starred-image` (`genpage.css:1908`), `.preset-remove-button` (`genpage.css:643`) | after an upstream merge, any of these resting values may have moved — a neutralized hover that no longer matches its resting state shows as a control stuck in the *wrong* colour rather than a latched one. Also re-check that `.model-selected` / `.browser-folder-tree-part-selected` are still the selected-state classes (they are excluded so a latched hover can't erase a real selection), and that the card menu button + pasted-image remove button still rest at `opacity: 0.7` rather than 0 (if either drops to 0, it becomes unreachable on touch and needs an explicit reveal) |
| `layout.js` `getKeyboardInset()` ↔ `AdvancedPopover.reposition()` | The keyboard lift is upstream's, as of the 2026-08 shell retirement: `getKeyboardInset()` → `--mobile-keyboard-inset` → `.mobile-keyboard-pin #alt_prompt_region { position: fixed; bottom: ... }`. The popover's own visual-viewport band math must stay in the same coordinate space. The fork's parallel `--kb-inset`/`kb-open`/`kb-animating` system is deleted — there is one keyboard system now, and it must stay that way | The formula is exact for a `position: fixed` consumer and must not gain a minimum-size floor: `iOS` scrolls the layout viewport under an open keyboard, so `offsetTop` grows and the still-required inset shrinks toward 0. Upstream's original `< 60` floor discarded real lifts mid-scroll (measured: 40px still needed at `offsetTop` 340 of a 380px keyboard), which un-pinned the bar back under the keyboard. Only a rounding epsilon (`< 2`) may be discarded; `mobilePromptFocused` is the real gate. If upstream ever re-raises that floor in a merge, re-apply the epsilon |
| `Assets/m/index.html` (standalone client page) | site.js top-level `new InputBrowserHelper()` (`site.js:1142`) needing `#input_image_browser_upload_container`; `showError` needing `center_toast`/`error_toast_box`/`error_toast_content`; script hard-order jquery→bootstrap→util→translator→permissions→site; `sendJsonToServer`'s relative `API/` URL + `getWSAddress` last-slash strip (why `/simple` has no trailing slash and hashes carry no `/`) | after upstream merges: load `/simple` once and check the console for boot throws ("/simple boot smoke"). **2026-09-01:** script hard-order unchanged (the fork's own `f5c81adf`/`9fe32c62` commits only moved the "More" link/nav button markup, not the `<script>` block); loaded `/simple` on a fresh throwaway server/fresh tab and got zero uncaught boot errors (the only console line is a Service-Worker-registration failure from the automated browser sandbox itself, caught by `mobile_core.js`'s own `.catch()` — `/sw.js` serves fine when fetched directly). No fix needed |
| `Assets/m/m_ui.js` (`showError` override) | `showError` still being a **plain global function declaration** in `site.js` (not a `const`, not a module export, not an object method) — that is what makes the file-scope reassignment reach every unqualified caller inside site.js; and the `ui.HideErrorMessages` filter semantics (`\|`-separated, substring match), which the override re-implements rather than delegating to, since delegating would show the toast it replaces | if upstream re-declares `showError` as `const`/`let`, the reassignment throws at load and takes `/simple` boot with it; if it moves to a module or an object, the override silently stops applying and errors go back to the center toast. Either way: generate against a stopped backend on `/simple` and confirm the failure lands in the header strip. If upstream changes the filter's separator or matching, port it here — a stale copy silently shows errors the user asked to hide. **2026-09-01:** `b24d62b3` did not touch `showError` (site.js:88) at all — still a plain global `function showError(message)`. No fix needed |
| `Assets/m/m_state.js` | Ported logic: `applyOnePreset` merge semantics (`presets.js:460-491` — `{value}`+segment split, loras concat), `copy_current_image_params` reuse mapping (`currentimagehandler.js:495-597` — `original_prompt` preference, `prompted_loras` pruning), `parameter_remaps`; server behavior: presets applied after raw params, LIST split on `\n|||\n`/comma, loras/loraweights index-align auto-repair | diff `/simple` vs desktop `sui_image_params` for one real preset after merges. **2026-09-01:** `T2IPromptHandling.cs` gained a `<weight[...]:>` tag processor, changed `<alternate:...>`'s internal serialization, and — new — calls `LegacyPromptParser.Convert()` on the raw prompt (gated by the new `Settings.cs` `ParamParsing.ParseAlternativePromptSyntaxes`, default `true`) to auto-convert Comfy/A1111 `(word:1.5)`/`[a\|b]`/`[from:to:when]` syntax into those tags before any tag processing runs. This is entirely server-side and applies uniformly to prompts submitted from `/simple` — no `m_state.js` change needed. Confirmed `LegacyPromptParser` only rewrites parens/brackets and recurses into `<...>` tag *bodies*; it never touches the tag name, so `<lora:...>` and `<trigger>` pass through unmodified |
| `Assets/m/m_state.js` (resolution math) | `MState.AspectReferences` is a copy of `T2IParamInput.ResolutionAspectReferences`, and `resolutionFor` mirrors `GetImageWidth`/`GetImageHeight` (`ref * sideLength / 512`, rounded to 16). Also depends on aspect-ratio-plus-side-length taking precedence over raw width/height server-side, on `RawResolution` outranking both (unused here, but it would override our width/height), and on `ListT2IParams` returning `model_classes[classId].standard_width` | if the table or the rounding changes upstream, the on-screen `W x H` readout silently lies while generation stays correct; if the precedence changes, `buildGenInput` deleting width/height becomes wrong. If upstream ADDS a ratio to its table that `MState.ExtraAspects` also defines, the fork copy wins for nothing - drop the duplicate so it goes back to being server-computed |
| `Assets/m/m_autocomplete.js` | Ported from `prompttools.js` (match/sort buckets, `<tag:` prefix set, the splice-at-index apply) and the parse loop in `main.js loadUserData`; `GetSimpleAutocompletions.autocompletions` retains the `GetMyUserData` line format (`name\nlow\ntag\ncount\nalts`); `GetUserSettings` returning `autocomplete.matchmode`/`sortmode`; `genericRequest`/`sendJsonToServer` retaining the optional timeout argument across session renewal; `util.js` `getTextContent`/`setTextContent`/`setTextSelRange`/`largeCountStringify`; `site.css` `.tag-type-0..9` | a changed line format shows as garbled chips; new prompt-syntax tags upstream will not appear here until the prefix table is updated (it is a copy, not a read); first prompt focus must trigger one request, and an error/timeout must permit a later bounded retry. **2026-09-01:** this is exactly what happened — `b24d62b3` added a `weight[1.5]` prefix to `prompttools.js`'s `registerPrefixes()`, and it is not yet in `m_autocomplete.js`'s own `registerPrefixes()`. Expected staleness per this row's own note, not a break; nothing else in the port (line format, timeout handling, `.tag-type-0..9`) changed. Left unported — a follow-up feature port, not a fix |
| `T2IParamType.ToNet` + `T2IAPI.ListT2IParams(compact)` | Default API behavior and the original one-argument CLR overloads remain intact; only compact callers skip evaluating and serializing `loras.values`, while `models.LoRA` remains present for `/simple` compatibility maps and `<lora:` completion | after upstream changes to parameter serialization, confirm the default call still includes values and `/simple` still receives `models.LoRA` but no `loras.values` |
| `BasicAPIFeatures.GetMyUserData(includeAutocompletions)` + `MobileEnhancementsExtension.GetSimpleAutocompletions` + `AutoCompleteListHelper.GetDataAsync` + `Utilities.YieldJsonOutput` | The default user-data call retains the legacy unbounded synchronous list contract, and the original one-argument CLR overload remains for precompiled extensions. `/simple` passes false, then lazy-loads through the bounded extension API with a 15-second request timeout and bounded focus retry. `character_tags` substitution and loading share one atomic cache generation and require an exact same-directory `all_tags`; full-list formatting is single-flight; mobile source/entry/output/suffix limits and bounded negative caches remain in force; client disconnects cancel read, parse, format, clone, JSON traversal, and response-write work; the per-user throttle remains in force | verify default genpage autocomplete, root/nested sibling selection, first-focus load, oversized-list warning, timeout/retry, focus switching, concurrent cold loads, disconnect cancellation, rate limiting, and cache refresh |
| `Assets/m/m_create.js` (live preview ownership) | The preview grid is built detached in the constructor precisely because `mGen.onFrame` fires before any tab is built; `m_images.openViewer`/`urlToPath` are called cross-object | if the preview is ever moved back into `build()`, generating while deep-linked to another tab throws on the first frame |
| `Assets/m/m_create.js` (filename prefix row) | `mState.paramMeta['filenameprefix']` — the server-registered param ID from `src/BuiltinExtensions/FilenamePrefix/`; `'filenameprefix'` in `MCreate.coveredParams` | if the extension is disabled or the param is renamed, `paramMeta['filenameprefix']` goes absent and the row hides itself (graceful, by design) - but if `coveredParams` ever drops the id while the param still exists, the value renders twice: once as the dedicated row, once as a duplicate Advanced chip |
| `Assets/m/m_gen.js` + `m_images.js` + `m_models.js` | API shapes: `GenerateText2ImageWS` flat input + frame kinds + reusable socket + `${request_id}_${batch_index}` identity; `GridGenRun` full bool set + `outputType` strings; `ListImages` inline metadata + `getImageOutPrefix()` URL building; `ListModels` `dataImages:false` + `lora_default_weight`; `GetMyUserData` preset shape **and `starred_models`** (subtype → `[name]`, the same store `SetStarredModels` writes and the genpage's star buttons drive, keyed by the exact name `ListModels` reports); `InterruptAll`/`GetCurrentStatus`; `ToggleImageStarred`/`DeleteImage` `{path}` | these are API couplings — they fail loud (errors/toasts), check after major upstream API changes. `starred_models` is the exception that fails *quiet*: a renamed key or a changed name format just stops sorting favourites first, with nothing on screen to say so — after an upstream merge touching user data, star a LoRA in the full UI and confirm it leads the `/simple` picker |
| `Assets/m/m_app.js` (Restart server row) | `UpdateAndRestart`'s shape: `force: true` alone must keep meaning "restart without git-updating anything" (`doUpdateServer` defaulting false, update lists optional); the `restart` permission ID; `Program.RequestRestart()` still exiting 42 and the launch scripts still relaunching on it; `sw.js` still passing non-GET through untouched, which is the only reason the liveness probe cannot be answered from cache | if `doUpdateServer` ever defaults true, this button silently becomes "git update and restart" — a far more destructive action than its label promises, and the single most important thing to re-check here after an upstream merge. If `sw.js` starts intercepting POSTs, the probe can be served from cache and the watcher may reload into the old process or hang. Both fail quiet |
| `Assets/m/m_image_edit.js` ↔ `m_create.js`'s `wireReorder` | The image-tile tap-to-open-editor listener is only conflict-free because the tile's long-press+drag reorder gesture (`wireReorder`) arms on a 150ms hold, not a plain tap - a tap was a genuine no-op on the tile before this shipped. Also: `getImageOutPrefix()` for fetching a `kind: 'path'` prompt image same-origin onto the editing canvas (same coupling `m_create.js`'s own image strip already has); `mState.promptImages`'s `{kind, value}` shape, which Save always writes back as `{kind: 'data', value: <jpeg data URI>}` regardless of what kind the entry started as | if `wireReorder` ever grows a plain-tap behaviour of its own (not just long-press), it will silently fight the editor's tap-to-open listener - both are bound to the same tile with no coordination between the two files beyond this note. Re-open the editor on a `kind: 'path'` image after any change to `getImageOutPrefix()` or the output route and confirm it still loads (a broken fetch fails loud - `mUI.warn` - so this is a low-risk row, included for completeness) |
